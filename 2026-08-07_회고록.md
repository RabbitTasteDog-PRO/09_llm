# 2026년 8월 7일 회고

오늘은 `05_langchain`의 내용을 따라가며, LangChain으로 LLM 애플리케이션을 구성하는 흐름을 `Model I/O → LangSmith 추적 → Retrieval → RAG` 순서로 정리했다. 단순히 모델에 질문을 보내는 코드에서 시작해, 외부 문서를 검색하고 그 근거를 모델에 전달하며 실행 과정까지 관찰하는 구조로 확장해 보았다.

## LangChain은 구성 요소를 연결하는 프레임워크

LangChain은 답변을 직접 학습하거나 보관하는 모델이 아니라, 모델·프롬프트·출력 파서·검색기·도구 같은 구성 요소를 같은 방식으로 연결하는 프레임워크이다. 단순한 한 번의 모델 호출은 OpenAI SDK만으로도 가능하지만, 모델 공급자를 교체하거나 검색·파싱·도구 실행을 이어 붙여야 할 때 LangChain의 장점이 커진다.

특히 각 단계가 주고받는 데이터 형태를 구분해서 보는 것이 중요했다. Prompt는 입력 딕셔너리를 문자열 또는 역할이 있는 메시지 목록으로 바꾸고, Chat Model은 이를 받아 `AIMessage`를 반환하며, Output Parser는 그 결과를 문자열이나 Python 객체로 바꾼다. 이 경계를 나누면 오류가 프롬프트 생성, 모델 호출, 출력 변환 중 어디에서 생겼는지 확인하기 쉽다.

```python
from langchain_core.output_parsers import StrOutputParser
from langchain_core.prompts import ChatPromptTemplate
from langchain_openai import ChatOpenAI

prompt = ChatPromptTemplate.from_messages(
    [
        ("system", "한국어로 간결하게 답한다."),
        ("human", "{question}"),
    ]
)

model = ChatOpenAI(model=CHAT_MODEL_NAME, use_responses_api=True)
chain = prompt | model | StrOutputParser()

answer = chain.invoke({"question": "LangChain은 무엇인가요?"})
print(answer)
```

`|` 연산자로 Prompt → Model → Parser를 연결하는 방식을 LCEL(LangChain Expression Language)이라고 한다. `prompt.invoke()`는 답변을 생성하는 호출이 아니라 변수와 메시지 역할이 채워진 모델 입력을 만드는 호출이라는 점도 다시 확인했다. 실제 답변을 받으려면 이후에 모델을 호출해야 한다.

## 모델 입력과 출력의 형태를 구분하기

`ChatPromptTemplate`에서는 `system` 메시지로 모델의 역할과 제약을 정하고, `human` 메시지로 사용자의 질문을 전달한다. few-shot은 역할을 나누는 것만을 뜻하지 않고, 새 질문에 앞서 입력과 원하는 출력 예시를 함께 제공해 모델이 형식이나 분류 기준을 따르게 하는 방법이다. 예시가 많아지면 토큰 비용과 지시 충돌 가능성도 커지므로, 필요한 대표 사례부터 사용하는 것이 좋다.

모델 호출 방식도 목적에 따라 달랐다. `invoke()`는 하나의 입력에 대한 최종 결과를 받고, `batch()`는 문맥을 공유하지 않는 여러 입력을 처리하며, `stream()`은 생성 중인 `AIMessageChunk`를 순서대로 받는다. 스트리밍 결과를 JSON처럼 완성된 형식으로 파싱하려면 모든 청크를 모은 뒤 Parser에 넘겨야 한다.

또한 `AIMessage` 전체에는 텍스트 외에도 사용량, reasoning, tool call 같은 정보가 들어갈 수 있다. 화면에 답변만 출력할 때는 `AIMessage.text` 또는 `StrOutputParser`를 사용하고, 도구 호출이나 대화 이력을 이어 가야 할 때는 메시지 객체를 보존하는 것이 적절하다.

구조화된 출력에서는 Pydantic `BaseModel`을 사용해 모델 응답의 데이터 계약을 만들 수 있었다. schema는 모델이 반환할 필드와 자료형을 검증하지만, 내용의 사실 여부까지 보장하지는 않는다. 따라서 형식 검증과 답변 품질 평가는 별도로 생각해야 한다.

```python
from pydantic import BaseModel, Field

class Recipe(BaseModel):
    title: str = Field(description="레시피 이름")
    ingredients: list[str] = Field(description="필요한 재료 목록")
    steps: list[str] = Field(description="조리 순서")

recipe_prompt = ChatPromptTemplate.from_messages(
    [
        ("system", "간결한 요리 도우미이다."),
        ("human", "{ingredient}로 만들 수 있는 요리를 추천해줘."),
    ]
)

recipe_chain = recipe_prompt | model.with_structured_output(Recipe)
recipe = recipe_chain.invoke({"ingredient": "토마토와 달걀"})

print(recipe.model_dump())
```

## LangSmith로 실행 과정 확인하기

LangSmith는 LangChain을 대체하는 도구가 아니라, LLM 애플리케이션의 실행을 trace로 기록하고 디버깅·평가하는 observability 도구이다. 한 번의 사용자 요청 전체가 trace이고, 그 안의 Prompt·Model·Parser·Retriever 같은 개별 작업이 run이다. 따라서 최종 답변만 확인하는 것보다, 각 단계의 입력·출력과 latency, error를 확인해 문제가 시작된 위치를 찾을 수 있다.

`LANGSMITH_TRACING=true`와 API Key, Project를 설정하면 LangChain 호출이 자동으로 기록된다. `with_config()`의 `run_name`, `tags`, `metadata`는 prompt 버전이나 실험 조건을 구분하는 데 사용하며, `tracing_context()`는 특정 블록에서만 trace를 다른 Project에 기록하게 한다. Prompt, Model, Parser는 각각 별도 run이므로 token과 cost는 실제 모델을 호출한 Model run에서 확인해야 한다.

```python
import langsmith as ls

traceable_chain = (prompt | model | StrOutputParser()).with_config(
    {
        "run_name": "langchain_practice_v1",
        "tags": ["practice", "prompt_v1"],
        "metadata": {"prompt_version": "v1"},
    }
)

with ls.tracing_context(project_name="langchain_practice", enabled=True):
    answer = traceable_chain.invoke({"question": "LCEL을 설명해줘."})

print(answer)
```

## Retrieval: 문서를 검색 가능한 지식으로 바꾸기

Retrieval은 질문과 관련된 외부 문서를 찾아 `list[Document]`로 반환하는 과정이다. LLM이 학습하지 않은 사내 문서나 최신 자료를 답변에 사용하려면, 질문을 바로 모델에 보내기 전에 관련 근거를 검색해야 한다. RAG는 Retrieval-Augmented Generation의 약자로, 검색된 문서를 문맥에 넣어 모델이 답변을 생성하는 방식이다. 환각을 완전히 없애지는 못하지만, 어떤 근거를 사용했는지 확인하고 문맥에 없는 내용은 모른다고 답하도록 제어할 수 있다.

전체 흐름은 다음과 같이 정리할 수 있었다.

`외부 데이터 → Document → chunk Document → embedding vector → Vector Store → Retriever → list[Document] → LLM 답변`

웹페이지나 PDF를 읽어 `Document`로 만들 때는 본문을 `page_content`에, URL·파일명·페이지 같은 추적 정보는 `metadata`에 저장한다. 긴 문서는 `RecursiveCharacterTextSplitter`로 chunk로 분할한다. 이때 `chunk_size`는 기본적으로 토큰 수가 아니라 문자 수 기준이며, `chunk_overlap`은 문맥 손실을 줄이는 대신 저장량과 임베딩 비용을 늘린다.

임베딩은 문장과 문단을 의미를 나타내는 숫자 벡터로 변환하는 과정이다. `all-MiniLM-L6-v2`를 로컬 CPU에서 사용했고, `normalize_embeddings=True`가 각 벡터의 L2(유클리드) 길이를 1로 정규화한다는 점을 확인했다. 정규화된 벡터끼리는 내적이 코사인 유사도와 같아지고, 코사인 유사도가 높은 순서와 유클리드 거리가 가까운 순서가 대응한다.

```python
from langchain_huggingface import HuggingFaceEmbeddings

local_embeddings = HuggingFaceEmbeddings(
    model_name="sentence-transformers/all-MiniLM-L6-v2",
    model_kwargs={"device": "cpu"},
    # 각 벡터의 L2(유클리드) 길이를 1로 정규화한다.
    encode_kwargs={"normalize_embeddings": True},
)
```

Vector Store는 임베딩 벡터와 원문·metadata를 저장하고 유사도 검색을 수행한다. InMemoryVectorStore는 간단한 실습에 적합하고, Chroma는 데이터를 디스크에 저장해 다시 불러와 검색할 수 있다. Retriever는 Vector Store를 질문 문자열을 받아 관련 `Document` 목록을 반환하는 인터페이스로 감싼 객체이다.

## 검색 문맥을 사용해 RAG 답변 만들기

마지막으로 ChromaDB에서 유사한 문서 3개를 검색하고, 출처와 페이지 정보를 포함한 문맥을 만들어 LLM에 전달하는 2-step RAG를 구성했다. system prompt에 “검색 문맥만 근거로 답하고, 문맥에 없으면 모른다고 답한다”는 규칙을 넣어 검색 근거 밖의 답변을 줄이도록 했다.

```python
db_retriever = reloaded_db.as_retriever(
    search_type="similarity",
    search_kwargs={"k": 3},
)

rag_prompt = ChatPromptTemplate.from_messages(
    [
        (
            "system",
            "검색 문맥만 근거로 한국어로 답한다. 문맥에 답이 없으면 모른다고 답한다. "
            "답변 마지막에 사용한 출처를 적는다.\n\n[검색 문맥]\n{context}",
        ),
        ("human", "{question}"),
    ]
)

rag_chain = rag_prompt | model | StrOutputParser()

def run_retrieval_rag(question: str) -> dict:
    documents: list[Document] = db_retriever.invoke(question)

    context_parts: list[str] = []
    for document in documents:
        source = document.metadata.get("source", "unknown")
        page = document.metadata.get("page", "web")
        context_parts.append(
            f"[출처: {source}, 페이지: {page}]\n{document.page_content}"
        )

    context_text = "\n\n---\n\n".join(context_parts)
    answer: str = rag_chain.invoke(
        {"context": context_text, "question": question}
    )

    # 마지막 document 하나가 아니라 검색된 목록 전체를 반환한다.
    return {"documents": documents, "answer": answer}
```

코드를 확인하면서 `return {"documents": document, ...}`처럼 반복문 안의 마지막 `document` 하나를 반환하는 실수를 발견했다. 이 경우 검색 결과 전체가 아닌 단일 `Document` 객체가 반환되고, 검색 결과가 없으면 `document`가 생성되지 않아 오류가 날 수 있다. 반환값에는 반드시 `documents` 목록을 사용해야 한다. 주석의 `context_pars`도 실제 변수명인 `context_parts`로 맞추는 것이 좋다.

오늘 학습을 통해 RAG는 단순히 LLM 앞에 검색을 붙이는 것이 아니라, 문서 수집·분할·임베딩·저장·검색·문맥 구성·답변 생성의 품질이 모두 연결된 구조라는 점을 알게 되었다. 다음에는 chunk 크기와 overlap, 검색 개수 `k`, 임베딩 모델을 바꾸었을 때 검색 문서와 최종 답변이 어떻게 달라지는지 비교하고, LangSmith trace와 평가 데이터를 이용해 근거 기반으로 RAG 품질을 점검해 보고 싶다.
