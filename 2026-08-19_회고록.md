# 2026년 8월 19일 회고

오늘은 LangGraph의 기본 그래프를 지나 `create_agent`와 다중 Agent 구조를 집중해서 봤다. 어제까지는 도구를 호출하고 대화 State를 이어 가는 흐름을 만들었다면 오늘은 역할이 나뉜 여러 Agent가 어떤 순서로 움직이고 언제 멈춰야 하는지까지 다뤘다. 노트북이 많아서 처음에는 각 코드가 따로 보였는데 마지막에는 모두 `State에 결과를 남기고 다음 Node를 고른다`는 흐름으로 이어졌다.

`Human-in-the-loop` 노트북에서는 `interrupt()`와 `Command(resume=...)`로 사람이 확인해야 하는 일을 중간에 멈추고 다시 시작하는 과정을 봤다. 도구 호출이 끝나는 지점과 사람에게 넘기는 지점은 비슷해 보여도 다르다. 도구는 바로 결과를 돌려주지만 사람 확인은 checkpoint에 멈춘 위치를 남긴다. 그래서 같은 `thread_id`와 함께 재개해야 원래 작업 흐름으로 돌아갈 수 있다.

```python
from langgraph.types import Command, interrupt


def human_assist(question: str) -> str:
    response = interrupt({"question": question})
    return response["answer"]


graph.invoke(
    Command(resume={"answer": "직원 확인 결과 구매 가능합니다"}),
    config={"configurable": {"thread_id": "cake-1"}},
)
```

`Custom State`도 다시 정리했다. `messages`에는 모델과 도구가 주고받은 대화가 쌓이고 검색어와 장소 목록 같은 값은 별도 필드에 저장한다. 모델에게 보여 줄 자연어와 이후 화면이나 다른 코드가 바로 쓸 구조화된 값은 용도가 다르다는 걸 알았다. Tool 호출 ID가 모델의 요청과 Tool 결과를 연결한다는 점도 같이 확인했다.

```python
class PlaceState(TypedDict):
    messages: Annotated[list[AnyMessage], add_messages]
    search_query: str | None
    search_results: list[dict[str, str]] | None
```

`create_agent()`를 다룬 노트북에서는 직접 `StateGraph`를 조립하지 않아도 Agent loop를 만들 수 있다는 걸 봤다. 그렇다고 내부의 State와 Tool loop가 없어지는 건 아니었다. `create_agent()`가 그 연결을 대신 해 주는 형태였다.

특히 실행마다 다른 prompt를 넣는 Middleware가 기억에 남는다. `@dynamic_prompt`는 모델 호출 직전에 실행되고 `context`로 받은 사용자 이름을 prompt에 넣을 수 있다. 여기서 `@`는 데코레이터라고 부르고 `@dataclass`는 데이터를 담을 클래스를 간단히 만드는 데코레이터다.

```python
@dataclass
class UserContext:
    user_name: str


@dynamic_prompt
def personalized_prompt(
    request: ModelRequest[UserContext | None],
) -> str:
    context = request.runtime.context

    if context is None:
        raise ValueError("UserContext가 필요합니다")

    return f"사용자를 {context.user_name}님이라고 부른다"
```

처음에는 `request.runtime.context.user_name`에서 경고가 났다. context가 비어 있을 수도 있다고 타입이 판단했기 때문이다. `None` 검사를 추가하고 `ModelRequest[UserContext | None]`처럼 Middleware가 받을 context 형태를 명시하니 왜 경고가 생겼는지 이해가 됐다. 타입 힌트는 코드를 바로 고쳐 주는 문법이 아니라 잘못된 상태가 들어올 가능성을 먼저 알려 주는 장치였다.

checkpointer를 붙인 Agent도 다시 보았다. `invoke()` 결과를 변수에 담으면 마지막 메시지를 가져올 수 있다. Agent 객체 자체가 메시지 딕셔너드가 되는 것은 아니었다.

```python
response = memory_agent.invoke(
    {"messages": [HumanMessage(content="서울 날씨 알려줘")]},
    config={"configurable": {"thread_id": "weather-thread-1"}},
)

print(response["messages"][-1].text)
```

다중 Agent에서는 같은 모델을 여러 개 쓰는 것보다 역할과 이동 규칙을 분리하는 일이 더 중요했다. 순차 그래프는 `Researcher → ChartGenerator`처럼 다음 Worker가 정해져 있다. 반면 Router는 사용자 요청에 따라 조사 Agent나 시각화 Agent로 갈라진다. Node는 실제 작업을 하고 Edge는 작업 다음에 어디로 이동할지 정한다. 둘 다 연결처럼 보여서 처음에는 같은 일이라고 생각했는데 역할이 분명히 달랐다.

```python
sequential_builder.add_edge(START, "Researcher")
sequential_builder.add_edge("Researcher", "ChartGenerator")
sequential_builder.add_edge("ChartGenerator", END)
```

Agentic RAG에서는 Retriever를 LLM의 답변 작성 전에 자료를 찾아 주는 사서처럼 이해했다. Retriever가 답을 만드는 게 아니라 관련 문서를 꺼내 주고 LLM이 그 문서를 바탕으로 답을 만든다. 검색 문서가 질문과 관련 있는지 평가한 뒤 답변을 만들거나 질문을 다시 쓰는 loop도 구성했다.

구조화된 출력에서 나온 타입 경고도 해결해 봤다. 실행 결과가 `dict`처럼 넓게 추론될 때는 `BaseModel`의 `model_validate()`로 원하는 형태를 검증하고 타입을 좁힐 수 있다. 파이썬 내장 함수는 아니고 Pydantic이 제공하는 검증 메서드다.

```python
class GradeDocuments(BaseModel):
    binary_score: Literal["yes", "no"]


raw_grade = structured_grader.invoke([HumanMessage(content=prompt)])
grade = GradeDocuments.model_validate(raw_grade)

return "generate_answer" if grade.binary_score == "yes" else "rewrite_question"
```

Supervisor 구조에서는 Researcher와 Coder가 작업을 끝내면 다시 Supervisor로 돌아가고 Supervisor가 `next`를 바탕으로 다음 경로를 고른다. `assert`로 Search 다음에는 WebScraper가 오고 모든 작업을 마치면 `FINISH`가 나오는지도 확인했다. `assert`는 조건이 거짓이면 `AssertionError`를 내서 예상한 route가 맞는지 빠르게 확인하는 테스트였다.

```python
class RouteDecision(BaseModel):
    next: Literal["Researcher", "Coder", "FINISH"]


def supervisor_node(state: SupervisorState) -> dict[str, str]:
    raw_decision = supervisor_chain.invoke({"messages": state["messages"]})
    decision = RouteDecision.model_validate(raw_decision)
    return {"next": decision.next}
```

마지막에는 ResearchTeam과 AuthoringTeam을 Subgraph로 묶는 계층형 구조까지 만들었다. Search와 WebScraper는 조사 팀 안에서 움직이고 NoteTaker와 DocWriter 그리고 ChartGenerator는 작성 팀 안에서 움직인다. Top Graph는 내부 Worker를 직접 보지 않고 팀 하나를 Node 하나처럼 취급한다. 팀 안의 복잡한 State를 마지막 message 하나로 감싸 상위 Graph에 전달하는 경계가 꽤 중요했다.

```text
TopSupervisor
    ↓
ResearchTeam
    ↓
AuthoringTeam
    ↓
FINISH
```

오늘 가장 오래 잡고 있던 부분은 타입 경고였다. 처음 입력에는 Supervisor가 정할 `next`가 아직 없다. 그런데 State에서 `next: str`로 필수값이라고 선언하면 `stream()`에 넣는 입력과 맞지 않는다는 경고가 난다. 이때 `RunnableConfig`를 입력 타입으로 쓰는 게 아니라 `TopState`에서 `next`를 `NotRequired[str]`로 만든다. `RunnableConfig`는 `config=`에 넣는 실행 설정이고 Graph 입력 State가 아니다.

```python
class TopState(TypedDict):
    messages: Annotated[list[AnyMessage], add_messages]
    next: NotRequired[str]


top_input: TopState = {
    "messages": [HumanMessage(content="사막여우를 조사해 줘")]
}

for event in super_graph.stream(
    top_input,
    config={"recursion_limit": 20},
    stream_mode="updates",
):
    print(event)
```

주피터에서는 코드를 고친 것만으로는 부족하다는 것도 다시 겪었다. `create_team_supervisor()`에서 `return supervisor`를 추가했어도 그 셀을 다시 실행하지 않으면 커널에는 예전 `None` 값이 남아 있다. `TopState`를 두 번 정의하면 이름은 같아도 Graph를 컴파일할 때 사용한 타입과 입력에서 쓴 타입이 달라져 경고가 계속 남는다. State 정의 셀부터 Graph 컴파일 셀까지 순서대로 다시 실행하거나 커널을 재시작한 뒤 전체 실행해야 했다.

오늘은 Agent가 많아질수록 모델을 더 똑똑하게 만드는 것보다 역할을 작게 나누고 State와 Edge 계약을 분명히 하는 쪽이 안정적이라는 걸 느꼈다. 다음에는 각 팀의 실패 상황과 파일 생성 실패도 State에 남기고 어떤 팀에서 멈췄는지 더 쉽게 확인할 수 있게 만들어 보고 싶다.
