# 2026년 8월 4일 회고

오늘은 LLM과 OpenAI API를 활용하는 기본 흐름을 직접 코드로 익혔다. Chat Completions에서 이전 대화를 `messages`에 누적해 문맥을 유지하는 방법, 스트리밍 응답을 청크 단위로 출력하고 완성된 답변을 다시 대화 이력에 저장하는 방법을 실습했다.

TTS로 음성 파일을 만들고, 생성한 MP3를 음성 인식 모델로 전사하는 과정도 확인했다. 파일 경로와 함수 인자, 변수 범위처럼 사소해 보이는 부분이 실제 실행에서는 큰 오류로 이어질 수 있다는 점을 배웠다.

또한 임베딩을 생성해 데이터프레임에 저장하고, 코사인 유사도로 질문과 가장 가까운 문서를 찾는 흐름도 이해했다. 단순히 API를 호출하는 것을 넘어, 입력·출력 형태와 벡터 차원을 확인하는 습관이 중요하다는 것을 느꼈다.

내일은 임베딩 검색 결과를 실제 LLM 답변 생성에 연결하는 RAG 흐름까지 구현해 보고 싶다.
