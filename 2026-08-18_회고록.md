# 2026년 8월 18일 회고

오늘은 LangGraph에서 대화형 Agent가 한 번 답하고 끝나는 구조를 넘어, `도구 호출 → 결과 확인 → 다음 판단`을 반복하고 그 과정을 저장하거나 사람의 확인으로 멈출 수 있게 만드는 방법을 정리했다. 처음에는 LangGraph가 LangChain의 도구 호출을 그래프로 보기 좋게 연결하는 도구 정도라고 생각했다. 하지만 실습을 이어 가면서 LangGraph의 핵심은 모델 응답 자체보다, Node 사이에서 어떤 상태를 전달하고 어느 시점에 다음 경로를 고를지 명시적으로 설계하는 데 있다는 점을 알게 됐다.

오늘 노트북에서 확인한 질문은 다음과 같았다.

- `LangGraph의 Node와 Edge를 설명하고 필요하면 검색 도구를 사용해줘`
- `반가워 내 이름은 김뀨뀨야` 뒤에 `내 이름이 뭐라고?`
- `4호 케이크가 홈페이지에서 구매가 되지 않는데, 구매 가능한지 직원에게 확인해줘`
- `남부터미널에서 맛집 목록을 찾아 줘`

질문은 서로 달라 보이지만 모두 같은 흐름으로 이어졌다. 모델이 현재 상태를 읽고, 필요한 경우 도구 실행을 요청하고, 결과를 다시 상태에 쌓은 뒤, 끝낼지 다음 단계로 갈지를 판단하는 구조이다.

```text
사용자 메시지
    ↓
chatbot Node
    ↓
AIMessage
    ├─ tool_calls 없음 → END
    └─ tool_calls 있음 → ToolNode → ToolMessage → chatbot Node
```

## `bind_tools()`는 도구를 실행하는 코드가 아니었다

가장 먼저 웹 검색 도구를 연결한 챗봇을 만들었다. `bind_tools()`를 호출하면 모델이 검색 도구의 이름, 설명, 입력 형식을 보고 `tool_calls`를 만들 수 있다. 여기서 처음 헷갈렸던 점은 모델이 도구를 알고 있다는 것과 도구를 실제로 실행했다는 것은 다르다는 점이었다.

모델이 반환하는 `AIMessage.tool_calls`는 `어떤 도구에 어떤 인자를 전달해 실행해 달라`는 요청이다. 실제 파이썬 도구 호출은 `ToolNode`가 담당하고, 실행 결과는 `ToolMessage`에 담아 다시 모델에게 전달해야 한다. 이 과정을 구분하지 않으면 검색 결과가 없는 상태에서 모델이 답을 만들거나, 도구 요청만 만든 뒤 흐름이 멈출 수 있다.

```python
from langchain_openai import ChatOpenAI
from langchain_tavily import TavilySearch

tavily_tool = TavilySearch(max_result=2)

llm = ChatOpenAI(
    model=OPENAI_CHAT_MODEL,
    use_responses_api=True,
)

tools = [tavily_tool]
llm_with_tools = llm.bind_tools(tools)
```

이 코드의 출력은 검색 결과가 아니라, 도구 사용 규칙이 추가된 새 모델 실행 객체이다. 실제 실행 전에 `tavily_tool.invoke({"query": "LangGraph에서 Node란 무엇인가?"})`로 도구만 따로 확인해 두면, API Key·입력 형식·네트워크 문제를 그래프 연결 문제와 분리할 수 있다.

## State는 Node 사이에 누적되는 대화 규칙이다

그래프의 각 Node는 이전 Node의 지역 변수를 공유하지 않는다. 대신 공통 State를 입력으로 받고, 자신이 바꾼 값만 반환한다. 오늘의 기본 State는 `messages` 하나였지만, 이 한 줄에 사용자 메시지, 모델의 도구 요청, 도구 실행 결과, 최종 답변이 모두 쌓인다.

```python
from typing import Annotated, TypedDict

from langchain_core.messages import AnyMessage
from langgraph.graph.message import add_messages


class State(TypedDict):
    messages: Annotated[list[AnyMessage], add_messages]
```

`add_messages`는 reducer이다. 기존 `messages`를 새 목록으로 덮어쓰지 않고 Node가 반환한 새 메시지를 순서대로 합친다. 따라서 검색이 한 번 실행된 경우 State에는 보통 다음과 같은 이력이 남는다.

```text
HumanMessage
→ AIMessage(tool_calls: tavily_search)
→ ToolMessage(tool_call_id: 같은 호출 ID)
→ AIMessage(최종 답변)
```

특히 `ToolMessage.tool_call_id`가 앞선 `AIMessage.tool_calls[i]["id"]`와 같아야 모델이 어느 요청의 결과인지 구분할 수 있다. 도구 이름만 같아도 여러 번 호출될 수 있으므로, 요청과 결과를 연결하는 기준은 이름보다 호출 ID라는 점을 확인했다.

## 조건부 Edge가 도구 반복을 만든다

`chatbot` Node는 현재 메시지 전체를 읽고 새 `AIMessage` 하나만 반환한다. 검색 도구를 직접 실행하지 않는다는 역할 분리가 중요했다. 도구 실행 여부는 별도 route function이 마지막 메시지의 `tool_calls`를 보고 판단한다.

```python
from langgraph.graph import END, START, StateGraph


def chatbot(state: State) -> dict:
    response = llm_with_tools.invoke(state["messages"])
    return {"messages": [response]}


def tools_condition(state: State):
    last_message = state["messages"][-1]
    has_tool_calls = bool(getattr(last_message, "tool_calls", []))
    return "tools" if has_tool_calls else END
```

이 함수를 `add_conditional_edges()`에 연결하면 `chatbot`이 끝날 때마다 다음 Node가 결정된다. 직접 만든 `ToolNode`에서는 허용된 도구 이름인지 검사하고, 각 요청의 `args`로 `tool.invoke()`를 호출한 다음 JSON 결과를 `ToolMessage`로 바꾸는 과정도 살펴봤다. 이후 LangGraph의 기본 제공 `ToolNode`를 사용해 같은 역할을 더 간결하게 구성했다.

```python
from langgraph.prebuilt import ToolNode

workflow = StateGraph(State)
workflow.add_node("chatbot", chatbot)
workflow.add_node("tools", ToolNode(tools))

workflow.add_edge(START, "chatbot")
workflow.add_edge("tools", "chatbot")
workflow.add_conditional_edges(
    "chatbot",
    tools_condition,
    {"tools": "tools", END: END},
)

graph = workflow.compile()
```

여기서 `tools → chatbot` Edge가 빠지면 도구 결과가 모델에 전달되지 않는다. 반대로 `chatbot → tools`를 무조건 연결하면 도구 요청이 없는 일반 답변에서도 잘못된 실행을 시도한다. 그래서 `조건부 Edge`는 단순한 if문이 아니라, 실행 경로를 안전하게 제한하는 규칙이라고 이해하게 됐다.

## Checkpoint는 같은 `thread_id`의 State를 다시 불러온다

다음으로 같은 대화를 다음 요청까지 기억하게 만들었다. `InMemorySaver`를 checkpointer로 컴파일하고, 실행할 때 `thread_id`를 전달하면 그래프가 각 단계의 State를 해당 대화 단위로 저장한다.

```python
from langgraph.checkpoint.memory import InMemorySaver

checkpointer = InMemorySaver()
graph = workflow.compile(checkpointer=checkpointer)

user_config = {
    "configurable": {"thread_id": "user1"},
}

graph.invoke(
    {"messages": [("human", "반가워 내 이름은 김뀨뀨야")]},
    config=user_config,
)

result = graph.invoke(
    {"messages": [("human", "내 이름이 뭐라고?")]},
    config=user_config,
)
```

두 번째 `invoke()`에 첫 문장을 다시 넣지 않아도 같은 `thread_id`의 checkpoint에서 메시지 이력을 복구한다. 반면 `user222`처럼 다른 `thread_id`를 사용하면 같은 질문이어도 이전 이름을 알 수 없다. 즉, 모델이 이름을 장기 학습한 것이 아니라 저장된 메시지 목록을 다시 문맥으로 전달받은 것이다.

```text
thread_id = user1
첫 번째 요청 → [이름 소개, 모델 응답] 저장
두 번째 요청 → 저장된 이력 + 새 질문 → 이름 회상

thread_id = user222
새 대화 이력 + 같은 질문 → 이전 이름을 알 수 없음
```

`InMemorySaver`는 현재 주피터 서버 메모리에만 저장되므로 서버나 커널이 종료되면 대화도 사라진다. 실제 서비스에서 대화를 유지하려면 외부 저장소 기반 checkpointer를 선택하고, 사용자·대화 단위가 섞이지 않도록 `thread_id` 생성 규칙도 함께 설계해야 한다.

또 `graph.get_state(config)`가 반환하는 `StateSnapshot`으로 마지막 저장 상태를 확인했다. `snapshot.values["messages"]`는 저장된 대화 이력이고, `snapshot.next`가 빈 tuple이면 실행할 Node가 남지 않아 `END`까지 완료된 상태라는 뜻이다.

## Human-in-the-loop은 사람에게 질문을 넘긴 뒤 같은 실행을 재개한다

케이크 구매 가능 여부나 가격처럼 자동으로 확정하면 안 되는 정보는 사람에게 확인을 요청하도록 만들었다. `interrupt()`는 현재 실행을 멈추고, 호출자에게 보여 줄 JSON 형태 payload를 반환한다. 이때 payload는 사람의 답변이 아니라, 사람에게 전달할 질문이다.

```python
from langchain_core.tools import tool
from langgraph.types import interrupt


@tool
def human_assist(query: str) -> str:
    """자동으로 확정할 수 없는 문의를 사람에게 전달한다."""
    human_response = interrupt({"query": query})
    return human_response["data"]
```

그래프를 처음 호출하면 `human_assist`에서 멈추고 `__interrupt__`에 `{"query": ...}`가 담긴다. 이 상태도 checkpointer가 같은 `thread_id`에 저장한다. 담당자가 확인한 답을 `Command(resume=...)`로 넘기면 새 사용자 메시지를 만드는 것이 아니라, 중단된 `interrupt()`의 반환값을 공급하면서 정확히 멈췄던 실행을 이어 간다.

```python
from langgraph.types import Command

resume_command = Command(
    resume={
        "data": "홈페이지 오류가 복구되어 4호 케이크 구매가 가능합니다. 가격은 50,000원입니다."
    }
)

response = graph.invoke(resume_command, config=config)
```

이 흐름은 `사람이 답변을 작성한다`는 기능만이 아니라, 중단된 작업의 위치까지 저장하는 구조이다. `snapshot.next`가 비어 있지 않으면 그래프가 끝난 것이 아니라 특정 Node에서 대기 중이라는 뜻이다. 또 재개할 때 `interrupt()`가 있는 Node는 처음부터 다시 실행될 수 있으므로, interrupt 앞에 결제나 파일 저장처럼 중복되면 문제가 되는 작업을 두지 않거나 멱등하게 설계해야 한다는 점도 중요했다.

```text
사용자 문의
→ chatbot
→ AIMessage(human_assist 호출)
→ ToolNode
→ interrupt({"query": ...})
→ 사람 검토
→ Command(resume={"data": ...})
→ ToolMessage
→ chatbot의 최종 안내
```

## Custom State에서는 대화와 업무 데이터를 나눠 저장한다

마지막으로 장소 검색 예제를 통해 메시지 이력 외의 업무 데이터를 State에 추가하는 방법을 정리했다. 대화용 `messages`만 사용하면 최종 답변 안에서 장소 이름과 주소를 다시 파싱해야 한다. 검색어와 장소 목록을 별도 key로 두면 지도 표시, 검색 로그, API 응답 같은 후속 코드가 자연어를 다시 해석하지 않고도 값을 사용할 수 있다.

```python
class KakaoPlace(TypedDict):
    name: str
    address: str
    url: str


class State(TypedDict):
    messages: Annotated[list[AnyMessage], add_messages]
    search_query: str | None
    search_results: list[KakaoPlace] | None
```

`messages`는 `add_messages` reducer로 누적하고, `search_query`와 `search_results`는 reducer를 지정하지 않아 새 결과로 교체한다. 한 요청의 목표 흐름은 아래와 같다.

```text
사용자 자연어 요청
→ extract_keywords 도구
→ search_query: "남부터미널 맛집"
→ kakao_search_place 도구
→ search_results: [{name, address, url}, ...]
→ 최종 AIMessage
```

이 구조에서 모델은 여전히 `messages`에 담긴 도구 결과를 읽고 답한다. 반면 프로그램은 `search_results`를 직접 사용한다. 같은 결과를 두 형태로 보존하는 이유는 사용자에게 보여 줄 자연어와 프로그램이 재사용할 구조화된 데이터의 목적이 다르기 때문이다.

오늘 내용을 한 문장으로 정리하면, LangGraph Agent는 모델에 도구를 하나 붙이는 기능이 아니라 `State`, Node, 조건부 Edge, checkpoint를 이용해 도구 실행·대화 기억·사람 검토·업무 데이터를 재현 가능하게 연결하는 실행 구조라는 점이다. 다음에는 `search_query`와 `search_results`를 실제 capture Node에서 갱신하고, 도구 오류·사람 미응답·잘못된 재개 요청까지 테스트해 보고 싶다.
