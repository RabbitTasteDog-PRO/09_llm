# 2026년 8월 5일 회고

오늘은 OpenAI API의 Embeddings, Moderation, Function Calling 기능을 차례로 학습하며, LLM 기능을 실제 애플리케이션 흐름으로 연결하는 방법을 정리했다. 단순히 모델에 질문을 보내 답을 받는 데서 끝나는 것이 아니라, 관련 정보를 검색하고 안전성을 확인하며 필요한 외부 기능을 호출하는 과정이 함께 필요하다는 점을 이해했다.

## Embeddings: 의미를 비교해 관련 문서 찾기

임베딩은 텍스트를 고정 길이의 실수 벡터로 변환하는 방식이다. 숫자 하나가 특정 단어의 뜻을 뜻하는 것은 아니지만, 벡터 전체의 패턴을 비교하면 문장의 의미적 거리를 계산할 수 있다. `text-embedding-3-small` 모델로 문장을 1,536차원 벡터로 만들고, 리뷰의 제목과 본문을 하나의 검색 문서로 결합한 뒤 문서별 벡터를 저장하는 과정을 실습했다.

질문과 FAQ 문서를 `input=[convert_question, *faq_documents]`로 한 번에 임베딩하는 방법도 확인했다. `*faq_documents`는 FAQ 문서 목록을 각각 펼쳐 질문 뒤에 이어 붙이는 문법이다. 따라서 응답의 첫 번째 벡터는 질문에, 나머지 벡터는 FAQ 문서 순서에 대응한다.

벡터의 길이를 1로 정규화한 뒤 내적하면 코사인 유사도를 구할 수 있다. `normalized_vectors[1:] @ normalized_vectors[0]`은 여러 FAQ 벡터와 질문 벡터의 내적을 한 번에 계산해 FAQ별 유사도 점수를 만든다. 이때 `(FAQ 수, 1536) @ (1536,)` 형태가 되며, `np.argmax`로 가장 높은 점수의 위치를 찾으면 같은 인덱스의 FAQ를 선택할 수 있다. 유사도 점수가 높더라도 실제 질문과 문서가 관련 있는지 확인해야 하며, 검색 품질은 데이터와 모델을 함께 검증해야 한다.

행렬곱과 전치의 규칙도 다시 확인했다. 행렬곱은 왼쪽 행렬의 열 수와 오른쪽 행렬의 행 수가 같을 때 가능하다. 여러 질문과 여러 FAQ의 모든 유사도를 구할 때는 `(질문 수, 임베딩 차원) @ (FAQ 수, 임베딩 차원).T`처럼 FAQ 행렬을 전치하여 `(질문 수, FAQ 수)` 형태의 점수 표를 만들 수 있다.

## Moderation: 입력과 출력의 안전 신호 확인하기

Moderation API는 텍스트나 이미지에 검토가 필요한 콘텐츠 신호가 있는지 확인하는 기능이다. 전체 판정인 `flagged`와 범주별 Boolean, 범주별 점수를 함께 확인할 수 있으며, 결과를 단순한 정답처럼 해석하기보다 서비스 정책과 사람의 검토를 위한 신호로 사용해야 한다.

텍스트 입력뿐 아니라 외부 이미지 URL 또는 Base64 형식의 로컬 이미지도 moderation 요청에 포함할 수 있다는 점을 배웠다. 또한 자해, 집단 폭력, 성적 콘텐츠처럼 민감한 예시에서 범주별 결과를 읽는 방법을 확인했다. Responses API에서는 생성 요청과 함께 입력·출력 moderation 신호를 확인할 수 있으므로, 사용자 입력과 모델 답변 양쪽을 모두 점검하는 흐름을 구성할 수 있다.

## Function Calling: 모델의 요청과 애플리케이션의 실행을 연결하기

Function Calling은 모델이 필요한 함수를 스스로 실행하는 기능이 아니라, 사용할 함수 이름과 JSON 형식의 인자를 애플리케이션에 요청하는 방식이다. 애플리케이션은 허용된 함수인지, 인자 형식과 권한이 올바른지 검증한 뒤 로컬 함수나 외부 API를 실행한다. 실행 결과는 `tool` 또는 `function_call_output` 형태로 다시 모델에 전달되고, 모델은 그 결과를 바탕으로 최종 자연어 답변을 만든다.

날씨 조회 예제를 통해 함수의 입력 스키마를 정의하고, 모델이 반환한 `tool_calls`를 확인한 뒤, 허용된 함수를 실행해 결과 JSON을 대화 이력에 추가하는 흐름을 살펴보았다. 한 번의 응답에 여러 함수 호출이 포함될 수 있으므로 각 호출을 순회해야 하며, 모델이 만든 함수명과 인자는 신뢰하지 않고 서버 측에서 반드시 검증해야 한다는 점이 중요했다. Chat Completions의 도구 호출 루프와 Responses API의 함수 호출 방식 모두 같은 원리로 동작한다는 것도 확인했다.

오늘 배운 기능을 연결하면, 음성 질문을 텍스트로 전사한 뒤 Moderation으로 안전 신호를 확인하고, Embeddings로 관련 FAQ를 검색한 다음, 필요한 경우 Function Calling으로 외부 정보를 가져와 답변을 만드는 애플리케이션을 구성할 수 있다. 다음에는 이 흐름을 직접 실행하면서 검색 결과, moderation 정책, 함수 호출 결과가 최종 답변에 어떻게 반영되는지 확인해 보고 싶다.
