# 2026년 8월 20일 회고

오늘은 MCP를 처음부터 끝까지 연결해 봤다. 처음에는 AI 채팅에 도구를 하나 추가하는 설정 정도로 생각했는데 막상 해보니 호스트, 클라이언트, 서버가 각각 다른 역할을 하고 있었다. 특히 설정 파일에 서버를 등록한 것과 서버 프로세스가 실제로 실행 중인 것은 전혀 다른 일이라는 점을 여러 번 확인했다.

MCP는 모델이 바로 파일이나 외부 서비스를 만지는 구조가 아니었다. AI Assistant나 GitHub Copilot 같은 호스트 안에 클라이언트가 있고 클라이언트가 서버에 연결한 뒤 서버가 공개한 도구를 호출한다. 같은 도구라도 서버가 로컬에서 실행되는지 외부에 이미 떠 있는지에 따라 설정과 조심해야 할 부분이 달랐다.

```text
사용자 요청
    ↓
AI Chat Host
    ↓
MCP Client
    ↓
MCP Server
    ↓
Tool 실행 결과
```

파일 시스템 서버는 로컬 STDIO 방식으로 연결했다. 호스트가 `npx`로 서버 프로세스를 시작하고 마지막 인자로 넘긴 폴더만 다룬다. 처음에는 작업 폴더 전체를 넣어도 되는지 잠깐 고민했는데 굳이 넓게 열 이유가 없었다. 실습 파일을 넣어 둔 `mcp_sandbox` 하나만 허용하는 편이 맞았다. MCP를 연결할 때 제일 먼저 보는 건 연결 성공이 아니라 어디까지 접근할 수 있느냐라는 말이 이제 조금 이해된다.

VS Code에서는 `mcp.json`의 형식도 다시 확인했다. PyCharm에서 쓰는 `mcpServers`와 비슷해 보이지만 VS Code는 최상위에 `servers`를 둔다. 같은 내용을 복사해서 쓰면 연결이 안 되는 이유가 여기 있었다.

```json
{
  "servers": {
    "courseFilesystem": {
      "type": "stdio",
      "command": "npx",
      "cwd": "/Users/choejiheum/SK_AI/09_llm/09_mcp",
      "args": [
        "-y",
        "@modelcontextprotocol/server-filesystem@2026.7.10",
        "/Users/choejiheum/SK_AI/09_llm/09_mcp/mcp_sandbox"
      ]
    },
    "context7": {
      "type": "http",
      "url": "https://mcp.context7.com/mcp"
    }
  }
}
```

파일 시스템 서버와 Context7은 둘 다 MCP 서버이지만 출발점이 다르다. 파일 시스템 서버는 내 컴퓨터에서 프로세스를 새로 실행한다. Context7은 이미 외부에서 실행 중인 HTTP 접속 주소에 연결한다. 그래서 파일 시스템 서버는 허용 폴더를 작게 잡는 것이 중요하고 Context7은 질문에 내부 코드나 키 같은 정보를 넣지 않는 것이 더 중요하다.

`.vscode` 폴더가 두 개 보여서 잠깐 헷갈리기도 했다. `09_llm/.vscode`는 `09_llm`을 작업공간으로 열었을 때 읽고 `09_mcp/.vscode`는 `09_mcp`만 열었을 때 읽는다. 설정 파일이 있다고 무조건 적용되는 게 아니라 지금 VS Code에서 어느 폴더를 최상단 작업공간으로 열었는지까지 맞아야 했다. 겉으로는 폴더 하나 차이인데 설정이 안 보일 때는 꽤 큰 차이였다.

직접 만든 Math 서버도 실행해 봤다. 터미널에서 실행하는 명령은 아래처럼 단순하다. `mcp.run()`은 파일 안의 코드이지 터미널 명령어에 넣는 단어가 아니었다.

```bash
cd /Users/choejiheum/SK_AI/09_llm/09_mcp/servers
python math_server.py
```

여기서 한 번 더 중요한 차이가 나왔다. `MCPServer.run()`은 기본 전송 방식이 STDIO다. 그런데 노트북의 파이썬 클라이언트는 `http://127.0.0.1:8001/mcp`에 연결하도록 작성되어 있었다. 그대로 실행하면 서버는 STDIO로 기다리고 클라이언트는 HTTP 주소로 접속하려 하니 둘이 만날 수 없다. 노트북에서 의도한 구조를 만들려면 서버도 Streamable HTTP로 실행해야 한다.

```python
from mcp.server import MCPServer


mcp = MCPServer("math_tools")


@mcp.tool()
def add(a: float, b: float) -> float:
    """두 숫자를 더한 뒤 10을 곱해 반환한다"""
    return (a + b) * 10


if __name__ == "__main__":
    mcp.run(
        transport="streamable-http",
        host="127.0.0.1",
        port=8001,
        streamable_http_path="/mcp",
    )
```

STDIO 서버에 일반 `print()`를 남기면 안 된다는 것도 같이 알았다. 표준 출력은 MCP 메시지가 지나가는 통로라서 `hello world` 같은 일반 출력이 섞이면 클라이언트가 프로토콜 메시지로 읽다가 문제가 생길 수 있다. 로그가 필요하면 표준 오류를 쓰거나 개발용 로그 설정을 따로 두는 편이 안전하다.

노트북 클라이언트는 서버를 자동으로 시작하지 않는다. 실행 중인 접속 주소에 새 클라이언트를 열고 도구 목록을 확인하거나 도구를 호출할 뿐이다. `async with` 블록을 사용하면 연결을 열고 닫는 경계가 코드에 보인다.

```python
from mcp import Client


MATH_MCP_URL = "http://127.0.0.1:8001/mcp"


async def call_add(a: float, b: float):
    async with Client(MATH_MCP_URL) as client:
        return await client.call_tool(
            "add",
            {"a": a, "b": b},
        )


result = await call_add(3, 7)
assert result.is_error is False
assert result.structured_content == {"result": 100.0}
```

서버를 멈췄다가 다시 시작하는 과정도 생각보다 분명했다. AI Assistant에 URL을 등록했다고 서버가 계속 살아 있는 건 아니다. 서버를 다시 실행한 뒤 호스트에서는 재연결을 하고 노트북에서는 위 함수처럼 새 클라이언트 연결을 다시 열어야 한다. 연결이 실패했을 때 무작정 코드를 고치기보다 지금 죽은 것이 설정인지 서버 프로세스인지 클라이언트 연결인지 순서대로 봐야겠다는 생각이 들었다.

Copilot에서 `Requested model not available for session` 오류도 만났다. 처음에는 MCP 설정이 틀린 줄 알았는데 요청이 도구까지 가지도 못하고 선택한 모델 단계에서 막힌 경우였다. 새 대화를 열고 모델을 `Auto`로 바꾸는 것부터 확인해야 했다. AI 채팅에서 오류가 보인다고 모두 도구나 서버 문제는 아니었다.

오늘은 MCP가 단순히 도구 목록을 늘리는 기능이 아니라 모델의 요청과 실제 시스템 사이에 경계를 만드는 방식이라는 걸 배웠다. 서버가 어떤 전송 방식으로 실행되는지, 누가 프로세스를 시작하는지, 어느 폴더와 어떤 URL까지 열어 둘지까지 계약의 일부였다. 다음에는 Math 서버를 HTTP와 STDIO 두 방식으로 각각 연결해 보고 같은 `add` 도구가 호스트에 따라 어떻게 발견되고 호출되는지 더 정확히 비교해 보고 싶다.
