
# 8월 2주차 회고록 : RAG, LangChain

이번 주는 랭체인의 에이전트와 기본 RAG에서 시작해, 와인 추천, 검색 최적화, 그래프 RAG까지 이어서 살펴봤다. 처음에는 도구 호출, 벡터 데이터베이스, BM25, 네오포제이가 전부 다른 기능처럼 보였다. 하나 배우면 또 다른게 나와서 좀 정신없기도 했다. 근데 코드를 따라가다 보니 결국 같은 질문으로 모였다. `사용자의 질문에 맞는 근거를 어떻게 찾고, 그 근거를 답변으로 어떻게 연결할 것인가`였다.

아직은 낯선 객체 이름과 긴 코드가 한 번에 나오면 흐름을 놓칠 때가 있다. 그래서 이번 주에는 코드를 외우기보다 각 단계에서 `무엇을 입력받는지`, `무엇을 반환하는지`, `반환값이 어디로 가는지`를 계속 봤다. "이 값은 왜 여기로 들어가지?" 하고 한 번씩 멈춰 보는 식이었다. 아직 완전히 알겠다는건 아니지만, 적어도 어디를 봐야 할지는 감이 왔다.

## 에이전트와 기본 RAG 흐름 확인

에이전트와 도구를 연결하고, PDF 문서를 검색하는 기본 RAG를 구성했다. 둘 다 복잡한 기능 묶음처럼 보였는데, 흐름으로 놓고 보니까 조금 덜 막막했다.

대규모 언어 모델은 문장을 잘 만들지만 현재 날씨나 최신 웹 정보까지 직접 확인하진 못한다. 그래서 조회 기능을 도구로 만들어 에이전트에 등록해 봤다. 질문을 보고 필요한 도구를 골라 실행하고, 그 결과를 가지고 답을 만드는 흐름이었다. 도구를 여러 개 넣었다고 전부 실행되는 건 아니었다.

```python
from langchain.agents import create_agent

multi_tool_agent = create_agent(
    model=model,
    tools=[get_current_weather, tavily_search, calculate_age],
    system_prompt=(
        "질문에 맞는 실제 조회 도구를 사용한다. "
        "도구가 반환한 값만 근거로 답한다."
    ),
)

result = multi_tool_agent.invoke(
    {"messages": [{"role": "user", "content": "서울의 현재 날씨를 알려 줘."}]}
)
print(result["messages"][-1].content)
```

결과의 마지막 답변만 보면 에이전트가 잘 동작한 것처럼 보인다. 막상 학습하거나 오류를 찾을 때는 `tool_calls`와 `ToolMessage`를 같이 봐야 했다. 실제로 도구를 호출했는지, 어떤 값을 넣었는지, 도구가 뭘 돌려줬는지는 중간 메시지에 남기 때문이다. 그걸 안 보면 답이 그럴듯한데도 어디서 틀렸는지 찾기 어려웠다.

랭체인 표현식 언어의 `|`도 처음엔 그냥 코드를 이어 쓰는 기호인 줄 알았다. 왼쪽 결과가 오른쪽 입력으로 넘어가는 거였다. 그래서 프롬프트가 만든 값, 에이전트가 받는 메시지, 에이전트가 돌려주는 딕셔너리를 따로 봐야 했다. 일반 파이썬 함수를 체인에 넣을 때 `RunnableLambda`로 감싸니 입력과 출력 경계도 좀 더 잘 보였다.

```python
from langchain_core.runnables import RunnableLambda

agent_pipeline = (
    agent_prompt
    | RunnableLambda(lambda prompt: {"messages": prompt.to_messages()})
    | RunnableLambda(lambda agent_input: multi_tool_agent.invoke(agent_input))
    | RunnableLambda(lambda result: result["messages"][-1])
    | StrOutputParser()
)
```

기본 RAG도 같은 방식으로 봤다. 검색기는 답을 만들어 주는 게 아니라 질문과 관련된 `list[Document]`를 가져온다. 가져온 문서를 문맥으로 정리하고 질문과 함께 모델에 넘겨야 그제야 근거를 가진 답변이 나왔다.

```text
사용자 질문
→ 검색기
→ 관련 문서 목록
→ 문맥
→ 프롬프트와 대규모 언어 모델
→ 최종 답변
```

특히 `docs = splitter.split_documents(docs)`처럼 같은 변수 이름을 다시 쓰는 부분에서 많이 멈췄다. 오른쪽의 기존 `docs`를 먼저 읽어 문서를 나누고, 그 결과를 왼쪽 `docs`에 다시 저장하는 흐름이었다. 문법에는 문제가 없지만 학습할 때 역할이 섞이므로, 아래처럼 변수 이름을 나누는 편이 이해하기 좋았다.

```python
page_docs = loader.load()
chunk_docs = splitter.split_documents(page_docs)
```

결국 랭체인 문법을 먼저 외우는 것보다, 변수와 반환값이 다음 단계로 어떻게 넘어가는지 읽는 일이 먼저였다.

## 음식 이미지를 와인 검색어로 바꾸기

음식 이미지 기반 와인 추천 RAG도 만들었다. 색인 만들기, 검색 흐름, 화면 연결까지 이어서 보니 생각보다 확인할게 많았다. 추천 결과만 보면 단순해 보이는데, 그 앞단이 꽤 길었다.

처음에는 파인콘이 와인을 추천해 주는 도구인 줄 알았다. 실제로는 문서와 질문을 같은 임베딩 모델로 숫자 벡터로 바꾸고, 가까운 문서를 찾는 벡터 데이터베이스였다. 이번 실습에서는 `text-embedding-3-small`의 `1536`차원 벡터를 썼으니 파인콘 색인도 같은 차원에 코사인 유사도를 기준으로 맞춰야 했다. 문서를 넣을 때 쓴 모델과 질문을 검색할 때 쓴 모델도 같아야 했다.

```python
from langchain_openai import OpenAIEmbeddings
from langchain_pinecone import PineconeVectorStore

embeddings = OpenAIEmbeddings(model="text-embedding-3-small")

wine_vector_store = PineconeVectorStore(
    index_name="winemag-review-data",
    embedding=embeddings,
)
```

와인 잡지 CSV의 한 행을 `Document` 하나로 읽고, 100개씩 나누어 파인콘에 넣었다. 전체 자료를 다시 넣으면 문서가 중복될 수 있다. 실제로 서비스를 만든다면 고정 문서 ID와 색인 통계를 같이 관리해야겠다고 느꼈다. 한 번 잘못 돌리고 나면 나중에 왜 검색 결과가 이상한지부터 다시 봐야 할 것 같았다.

```python
wine_docs = CSVLoader(
    file_path="./winemag-data-130k-v2.csv",
    encoding="utf-8",
).load()

for start in range(0, len(wine_docs), 100):
    wine_vector_store.add_documents(wine_docs[start:start + 100])
```

음식 이미지를 바로 텍스트 리뷰 검색에 넣지 않았다는 게 여기서 제일 중요했다. 이미지는 먼저 시각 정보를 읽을 수 있는 모델에 전달해 재료, 조리법, 풍미를 담은 영어 문장으로 바꿨다. 이 문장이 와인 리뷰를 찾는 검색어가 됐다. RAG의 검색어가 꼭 사용자가 입력한 원문일 필요는 없었다. 저장된 문서와 잘 비교할 수 있게 바꾼 값이어도 됐다.

```text
음식 이미지 주소
→ 시각 정보를 읽는 대규모 언어 모델
→ 재료·조리법·풍미를 담은 영어 문장
→ 질문 임베딩
→ 파인콘에서 유사한 와인 리뷰 검색
→ 리뷰를 근거로 와인 추천
```

노트북에서 끝내지 않고 스트림릿 화면으로 연결하면서 입력 검증도 추가했다. 이미지 주소를 외부 서비스에 보내기 전 `https`인지, `localhost`·루프백 주소·사설 IP가 아닌지 확인했다. 비밀 키는 화면이나 오류 메시지에 보이지 않게 하고, 필요한 환경 변수 이름만 안내했다. 모델과 데이터를 연결했다고 끝이 아니었다. 어떤 입력을 어디서 막을지까지 생각해야 했다.

```python
def ai_sommelier_rag(*image_urls: str):
    chain = build_ai_sommelier_chain()
    return chain.stream({"image_urls": list(image_urls)})


with st.spinner("음식과 관련된 와인 리뷰를 분석중입니다..."):
    st.write_stream(ai_sommelier_rag(image_url))
```

이미지 기반 와인 추천은 모델에게 답을 바로 부탁하는 기능이 아니었다. `이미지 → 검색 가능한 풍미 문장 → 관련 리뷰 → 근거를 가진 추천` 이 흐름을 만드는 일이었다.

## 검색 방법을 비교하고 수치로 확인하기

검색 품질을 높이는 방법도 살펴봤다. 같은 문서와 질문으로 BM25, 밀집 검색, RRF, 메타데이터 필터, 자체 질의 검색기를 비교했다. 이름은 많았지만 결국 "이 질문에 필요한 문서를 제대로 찾았나?"를 확인하는 방법들이었다.

BM25는 질문과 문서에 함께 나온 단어로 점수를 계산하는 검색 방식이다. 한국어는 조사와 어미가 붙기 때문에 단순히 공백으로 나누기보다 `Okt`로 형태소를 나눠 썼다. 밀집 검색은 문서와 질문을 임베딩 벡터로 만들고 뜻이 가까운 문서를 찾는 방식이다.

```python
from konlpy.tag import Okt
from rank_bm25 import BM25Okapi

okt = Okt()
tokenized_corpus = [okt.morphs(content) for content in documents_df["content"]]
bm25 = BM25Okapi(tokenized_corpus)

def bm25_search(query: str, top_k: int = 5) -> list[str]:
    scores = bm25.get_scores(okt.morphs(query))
    ranked_indices = sorted(
        range(len(scores)), key=lambda index: scores[index], reverse=True
    )[:top_k]
    return [documents_df["doc_id"].iloc[index] for index in ranked_indices]
```

30개 질문으로 `P@5`, `R@5`, `MRR@5`, `MAP@5`를 계산해 보니 이번 자료에서는 밀집 검색이 가장 높았다. BM25와 밀집 검색 순위를 RRF로 합친다고 자동으로 좋아지진 않았다. 이미 밀집 검색의 첫 정답 순위가 좋았고, RRF가 일부 질문에서는 정답을 뒤로 밀었다. 그래서 `MRR`과 `MAP`이 조금 낮아졌다.

| 지표 | BM25 | 밀집 검색 | RRF |
| --- | ---: | ---: | ---: |
| P@5 | 0.246667 | 0.260000 | 0.260000 |
| R@5 | 0.883333 | 0.916667 | 0.916667 |
| MRR@5 | 0.966667 | 0.983333 | 0.966667 |
| MAP@5 | 0.849074 | 0.880741 | 0.857963 |

```python
def rrf_fuse(
    bm25_ranked_ids: list[str],
    dense_ranked_ids: list[str],
    rank_constant: int = 60,
) -> list[tuple[str, float]]:
    candidate_scores: dict[str, float] = {}

    for ranked_ids in (bm25_ranked_ids, dense_ranked_ids):
        for rank, doc_id in enumerate(ranked_ids, start=1):
            score = 1 / (rank_constant + rank)
            candidate_scores[doc_id] = candidate_scores.get(doc_id, 0.0) + score

    return sorted(candidate_scores.items(), key=lambda item: item[1], reverse=True)
```

여러 검색 방법을 썼다는 사실보다 실제 질문에서 검색 품질이 좋아졌는지가 더 중요했다. BM25는 고유명사나 정확한 기술 용어처럼 단어 일치가 중요한 경우에, 밀집 검색은 같은 뜻을 다른 말로 표현한 문장을 찾을 때 강점이 있었다. 그러니까 RRF를 붙였다고 무조건 더 좋아지는 건 아니었다.

의미가 비슷하다고 해결되지 않는 조건은 메타데이터로 처리했다. 문서의 `author`, `category`를 파인콘 메타데이터에 넣고 검색 전에 필터를 걸면, 조건에 맞지 않는 문서는 후보에서 빠진다.

```python
category_filter = {"category": {"$in": ["역사", "기술"]}}

results = vector_store.similarity_search(
    query="훈민정음",
    k=5,
    filter=category_filter,
)
```

자체 질의 검색기는 사용자의 자연어 질문에서 본문 검색어, 메타데이터 조건, 반환 개수를 나누어 `StructuredQuery`로 만드는 방식이었다. 예를 들어 `김민수가 작성한 검색 관련 문서 2개만 찾아줘`는 `검색 관련`이라는 본문 검색어, `author == 김민수`라는 조건, `2`라는 개수로 나뉜다. 이 과정에도 모델 호출이 들어가므로, 만들어진 `StructuredQuery`, 변환된 파인콘 필터, 최종 검색 결과를 각각 확인해야 한다.

```text
김민수가 작성한 검색 관련 문서 2개만 찾아줘
→ 본문 검색어: 검색 관련
→ 조건: author == 김민수
→ 반환 개수: 2
→ 파인콘 검색
```

검색 품질을 높이는 일은 더 큰 모델 하나를 고르는 일보다, 데이터와 질문에 맞는 검색 방법과 조건을 정하고 정답 문서로 검증하는 쪽에 가까웠다.

## 문서 압축과 그래프 RAG

문서 압축, 네오포제이, 그래프 RAG, 벡터·그래프 결합 RAG도 이어서 정리했다. 여기까지 오니 벡터 검색만으로는 안 되는 질문도 확실히 보였다. 관계 자체가 답인 질문이 있었다.

먼저 문서 압축과 문맥 압축의 차이를 구분했다. 문서 압축은 문서를 색인에 넣기 전에 전체 내용을 요약하거나 정리하는 방식이고, 문맥 압축은 질문이 들어온 뒤 검색된 문서에서 필요한 부분만 줄이는 방식이다. 이번 실습에서는 원문을 요약해 별도 색인에 넣고, 원문 색인과 압축 색인을 같은 질문과 정답 문서로 비교했다.

```python
summary_chain = summary_prompt | summary_llm | StrOutputParser()

compressed_texts = []
for _, row in documents_df.iterrows():
    compressed_texts.append({
        "doc_id": row["doc_id"],
        "content": summary_chain.invoke({"text": row["content"]}),
    })
```

요약문은 길이를 줄일 수 있지만 고유명사, 수치, 조건까지 사라지면 검색 품질이 떨어질 수 있다. 그래서 압축 여부는 글자 수만 보지 않고 원문과 압축본의 `Recall@5`, `MRR`, `MAP`을 같은 기준으로 비교해야 했다. 문서 ID를 파인콘의 기록 ID로 써서 같은 코드를 다시 실행해도 중복 대신 갱신되게 만든 것도 중요했다.

그래프 데이터베이스에서는 데이터 자체뿐 아니라 데이터 사이의 관계도 함께 저장한다. 네오포제이에 `Person`, `Organization`, `Course` 노드와 `WORKS_AT`, `OFFERS`, `TEACHERS` 관계를 만들고, 사이퍼로 관계 경로를 조회했다.

```python
write_query = """
UNWIND $rows AS row
MERGE (person:Person {name: row.person})
MERGE (organization:Organization {name: row.organization})
MERGE (course:Course {title: row.course})
MERGE (person)-[:WORKS_AT]->(organization)
MERGE (organization)-[:OFFERS]->(course)
MERGE (person)-[:TEACHERS]->(course)
"""

driver.execute_query(
    write_query,
    rows=graph_rows,
    database_=neo4j_database,
)
```

그래프 RAG에서는 `Text2CypherRetriever`가 자연어 질문을 그래프 구조와 예시를 참고해 사이퍼로 바꾸고, 네오포제이 조회 결과를 검색 근거로 반환한다. 벡터 RAG가 뜻이 가까운 문서를 찾는 방식이라면, 그래프 RAG는 `누가 어느 기관에서 어떤 강좌를 제공하는가`처럼 관계 자체가 답의 핵심일 때 적합했다.

```python
graph_retriever = Text2CypherRetriever(
    llm=llm,
    driver=driver,
    neo4j_schema=neo4j_schema,
    examples=examples,
    neo4j_database=neo4j_database,
)

retrieval_result = graph_retriever.search(
    query_text="민지가 소속된 기관에서 제공하는 강좌는 무엇인가?"
)
```

마지막으로 벡터·그래프 결합 RAG에서는 의미가 가까운 `Document` 후보를 먼저 찾고, 선택된 문서에서 `SUPPORTS` 관계를 따라 `Course` 정보를 확장했다. 여기서 결합은 벡터와 BM25의 점수를 합친다는 뜻이 아니라, 벡터 검색 결과에 그래프 관계 정보를 더하는 흐름이었다.

```text
자연어 질문
→ 질문 임베딩
→ 네오포제이 벡터 색인에서 문서 후보 검색
→ SUPPORTS 관계를 따라 강좌 정보 확장
→ 문서 본문과 관계 정보
→ 대규모 언어 모델 답변
```

```python
vector_graph_retriever = VectorCypherRetriever(
    driver=driver,
    index_name="lecture_document_embeddings",
    retrieval_query=retrieval_query,
    embedder=embedder,
    neo4j_database=neo4j_database,
)

hybrid_result = vector_graph_retriever.search(
    query_text="외부 지식과 관계를 이용해 답변 근거를 확장하는 방법은?",
    top_k=2,
)
```

답변이 기대와 다르면 검색과 생성을 나눠 봐야 한다는 기준도 생겼다. 먼저 벡터 검색이 알맞은 문서를 찾았는지, 다음으로 사이퍼가 관계를 제대로 확장했는지, 마지막으로 모델이 검색 문맥 안의 근거를 사용했는지를 순서대로 확인하면 됐다. 한 번에 다 고치려고 하면 어디가 문제였는지 더 안 보였다.

## 이번 주 회고

이번 주에는 RAG가 벡터 데이터베이스 하나를 붙인다고 끝나는 기능은 아니라는걸 여러 방향에서 봤다. 이미지는 문서와 비교할 수 있는 풍미 문장으로 바꿔야 했고, 검색은 질문 특성에 따라 BM25·밀집 검색·RRF·메타데이터 조건을 골라야 했다. 관계가 중요한 질문에는 그래프 RAG가 필요했다. 문서를 요약해 색인을 가볍게 만들 때도 검색 품질이 유지되는지 확인해야 했다. 한 단계만 틀어져도 다음 단계가 같이 흔들렸다.

아직은 랭체인 객체 이름과 사이퍼 문법이 익숙하지 않다. 그래도 코드 한 줄을 외우기보다, 아래 기준으로 보면 덜 헤맬 것 같았다.

- 이 단계의 입력은 무엇인가?
- 반환값은 어떤 자료형이며 어디로 전달되는가?
- 검색 결과와 최종 답변을 나누어 확인했는가?
- 새로운 방법이 실제 검색 품질이나 사용자 조건을 개선했는가?

다음에는 이미지 기반 와인 추천에서 기본 추천과 RAG 추천을 같은 입력으로 비교하고, 검색된 리뷰와 메타데이터를 화면에 같이 보여 주고 싶다. RRF의 후보 수와 `rank_constant`를 바꿔 수치 변화도 확인하고, 자체 질의 검색기가 만든 조건의 오류 사례도 모아봐야겠다. 그래프 RAG도 질문, 그래프 구조, 생성된 사이퍼를 같이 저장해서 관계 검색이 필요한 경우와 벡터 RAG만으로 충분한 경우를 비교해 보고 싶다.

이번 주를 한 문장으로 정리하면, 좋은 RAG는 모델 하나를 더 붙이는 일이 아니라 `입력을 검색 가능한 형태로 바꾸고, 알맞은 근거를 찾고, 그 근거가 실제 답변에 반영되는지 검증하는 과정`이었다. 다음에도 기능 이름부터 외우기보다는 이 흐름을 먼저 따라가 봐야겠다.
