# 2026년 8월 6일 회고

오늘은 04 폴더의 SFT 데이터 준비 내용과 05 폴더의 LangChain·LangSmith 내용을 연결해서, 모델을 호출하는 코드가 실제로 어떤 입력을 만들고 어떤 결과를 남기는지 확인했다. 처음에는 `ChatOpenAI`에 질문을 넣고 답을 받는 흐름이 중심이었다. 이후에는 Prompt, Parser, Pydantic schema, LangSmith trace를 붙이며 한 번의 모델 호출을 재현하고 점검할 수 있는 구조로 넓혀 보았다.

## 프롬프트, few-shot, SFT의 경계

오늘 가장 분명해진 점은 대화 이력, 프롬프트, few-shot, SFT가 모두 모델의 답변에 영향을 주지만 작동하는 위치가 다르다는 것이다. 이전 `user`·`assistant` 메시지를 다시 보내는 것은 대화 문맥을 유지하는 방법이고, `system` 메시지의 역할과 제약은 이번 요청의 행동을 지시하는 프롬프트이다. 둘 다 호출이 끝난 뒤 모델 가중치를 바꾸지는 않는다.

few-shot은 단순히 `system`, `user`, `assistant` 역할을 나누는 방식이 아니라, 새 입력과 비슷한 질문·답변 예시를 몇 개 보여 주는 방식이다. 역할과 제약만 제공하면 zero-shot이고, `user` 질문 뒤에 원하는 형식의 `assistant` 답변을 예시로 포함해야 few-shot이 된다. 예시는 정답 지식 자체를 저장하려는 목적보다 답의 형식, 분류 라벨, 길이, 풀이 방식처럼 이번 호출에서 따라야 할 패턴을 보여 주는 데 유용하다.

```python
from langchain_core.prompts import FewShotPromptTemplate, PromptTemplate

examples = [
    {"q": "2 + 2 = ?", "a": "4"},
    {"q": "3 + 5 = ?", "a": "8"},
]

few_shot_prompt = FewShotPromptTemplate(
    examples=examples,
    example_prompt=PromptTemplate.from_template("Q: {q}\nA: {a}"),
    prefix="다음 수학 문제는 정답만 출력한다.",
    suffix="Q: {question}\nA:",
    input_variables=["question"],
)

# Prompt의 invoke는 완성된 프롬프트를 만든다.
formatted_prompt = few_shot_prompt.invoke({"question": "123 + 456 = ?"})
print(formatted_prompt.text)

# 모델까지 연결해야 실제 답변을 받는다.
answer = (few_shot_prompt | model | StrOutputParser()).invoke(
    {"question": "123 + 456 = ?"}
)
print(answer)  # 579
```

04 폴더에서 배운 SFT는 사람이 검수한 `system → user → assistant` 모범 사례를 JSONL로 만들고, 학습 Job을 통해 모델의 가중치 또는 어댑터 파라미터를 바꾸는 방법이다. 따라서 말투나 출력 형식을 한 번 시험할 때는 프롬프트와 few-shot부터 사용하고, 여러 상황에서 반복되는 행동을 더 일관되게 만들 필요가 있을 때 데이터 품질과 평가 기준을 준비해 SFT를 검토해야 한다. 특히 모델이 만든 답을 검수 없이 학습 정답으로 쓰면 오류나 편향도 함께 학습될 수 있다는 점을 기억해야 한다.

## LangChain Model I/O를 나누어 보기

LangChain에서는 `Prompt → Model → Parser`를 각기 다른 단계로 다룬다. `PromptTemplate`과 `ChatPromptTemplate`은 변수 딕셔너리를 모델 입력으로 바꾸고, `ChatOpenAI`는 그 입력을 받아 보통 `AIMessage`를 반환하며, Output Parser는 그 결과를 문자열이나 Python 자료형으로 변환한다. 이 경계를 나누면 오류가 입력 생성, 모델 호출, 출력 변환 중 어디서 생겼는지 확인하기 쉬워진다.

`ChatPromptTemplate.from_messages()`에서는 메시지 역할을 `"system"`, `"human"` 또는 `"user"`, `"ai"` 또는 `"assistant"`처럼 허용된 소문자 값으로 작성해야 한다. 또한 Prompt 객체의 `invoke()`는 모델 답변을 만드는 것이 아니라 `StringPromptValue` 또는 `ChatPromptValue`를 만드는 호출이다. `formatted_few_shot.text`에 프롬프트만 보이고 정답이 없었던 이유도 여기에 있었다. 답을 받으려면 `model.invoke(formatted_few_shot)`을 한 번 더 호출하거나, `few_shot_prompt | model | StrOutputParser()`처럼 LCEL chain으로 연결해야 한다.

`invoke()`, `batch()`, `stream()`의 차이도 정리했다. `invoke()`는 입력 하나의 최종 `AIMessage`를 받고, `batch()`는 서로 문맥을 공유하지 않는 여러 입력을 병렬 처리해 입력 순서에 맞는 결과 목록을 받는다. `stream()`은 생성 중인 답변의 일부인 `AIMessageChunk`를 순서대로 준다. 청크는 반드시 한 글자나 한 토큰 단위가 아니며, 모두 합치면 완성된 메시지가 된다. 따라서 완성된 JSON이 필요한 Parser는 스트림을 모두 모은 뒤 적용해야 한다.

```python
# 입력 하나의 최종 결과
single_response = model.invoke("한국의 수도는 어디야?")
print(single_response.text)

# 서로 독립적인 여러 입력의 결과 목록
responses = model.batch(["2 + 2는?", "3 + 5는?"])
print([response.text for response in responses])

# 생성되는 텍스트 조각을 즉시 출력하고, 동시에 완성 메시지를 복원
full_message = None
for chunk in model.stream("한 문단으로 LangSmith를 설명해줘."):
    print(chunk.text, end="", flush=True)
    full_message = chunk if full_message is None else full_message + chunk

print("\n\n완성된 답변:", full_message.text)
```

Responses API를 사용하는 `ChatOpenAI`의 `AIMessage.content`에는 최종 텍스트 외에 reasoning이나 tool call처럼 구조화된 항목이 들어갈 수 있다. 특히 긴 `encrypted_content`는 사람이 읽을 답변이 아니라 암호화된 추론 상태이므로, 화면에는 보통 `response.text`만 출력하는 편이 적절하다. 모호한 질문은 답이 짧아도 후보를 고르고 조건을 판단하느라 더 많은 내부 추론과 시간이 필요할 수 있다. 이런 차이는 `usage_metadata`로 입력·출력·추론 토큰을 함께 확인해야 정확히 판단할 수 있다.

수식이 포함된 few-shot 질문에서는 Python 문자열의 백슬래시도 주의해야 했다. raw string이 아닌 일반 문자열에서 `\t`, `\f`, `\r`은 탭, 폼 피드, 줄바꿈처럼 해석될 수 있어 LaTex 수식이 깨진다. raw string을 사용하거나, 이번처럼 `∫`, `→`, `²` 같은 일반 수식 기호로 질문을 작성하는 방법을 선택할 수 있다. 또 prefix에서 “정답만 출력”을 지시하면서 질문에서는 “과정을 설명”하도록 요구하면 프롬프트 안에 충돌이 생기므로, 실험 목적에 맞게 한 가지 형식을 일관되게 지시해야 한다.

## 구조화된 출력은 데이터 계약을 만든다

`Recipe(BaseModel)`처럼 Pydantic `BaseModel`을 상속한 클래스는 모델 종류가 아니라 출력 데이터의 설계도(schema)다. `model.with_structured_output(Recipe)`에 `Recipe` 클래스를 그대로 전달하면 LangChain이 필드 이름, 자료형, `Field` 설명을 읽어 구조화된 출력 계약을 만들고, 모델 결과를 검증한 `Recipe` 객체로 돌려준다.

여기서 `Recipe`는 클래스이고, `Recipe(...)`는 제목·재료·순서가 채워진 실제 인스턴스다. 이는 `nn.Module`을 상속해 신경망의 구조를 정의한다는 점과 비슷하지만, `BaseModel`은 가중치나 `forward()`를 가진 계산 모델이 아니라 데이터 형식과 검증 규칙을 정의한다는 차이가 있다. schema를 통과했다고 해서 레시피의 내용이나 고객 문의 분류가 사실이라는 뜻은 아니므로, 05 폴더의 고객 문의 예제처럼 기대값과 별도로 비교·평가해야 한다.

```python
from pydantic import BaseModel, Field
from langchain_core.prompts import ChatPromptTemplate

class Recipe(BaseModel):
    title: str = Field(description="레시피 이름")
    ingredients: list[str] = Field(description="필요한 재료 목록")
    steps: list[str] = Field(description="조리 순서")

recipe_prompt = ChatPromptTemplate.from_messages([
    ("system", "간결한 요리 도우미이다."),
    ("human", "{ingredient}로 만들 수 있는 요리를 추천해줘."),
])

# Recipe는 실제 레시피가 아니라 모델 출력의 schema를 나타내는 클래스다.
recipe_chain = recipe_prompt | model.with_structured_output(Recipe)
recipe = recipe_chain.invoke({"ingredient": "토마토와 달걀"})

print(recipe.model_dump())
```

## LangSmith로 실행을 관찰하기

LangSmith는 LangChain을 대체하는 도구가 아니라, LLM 애플리케이션의 실행을 추적하고 비교·평가하는 observability 도구이다. `LANGSMITH_TRACING=true`, API Key, Project 설정이 준비되어 있으면 LangChain 모델과 chain 호출의 trace가 자동으로 남는다. Project는 trace를 구분하는 저장 단위이고, trace는 한 요청의 전체 실행 기록이며, run은 그 안의 Prompt·Model·Parser 같은 개별 단계다.

`with ls.tracing_context(project_name="My_test"):`는 모델을 바꾸거나 프로젝트 객체를 받아오는 코드가 아니다. 해당 블록 안에서 발생한 trace의 목적지 Project를 동적으로 바꾸는 컨텍스트 매니저다. 지정한 Project가 없다면 첫 trace가 수신될 때 생성될 수 있고, 블록을 벗어나면 `.env`의 기본 Project 설정으로 돌아간다. 따라서 `tracing_context`는 `langchain`이 아니라 `langsmith`에서 import해야 하며, `with`가 컨텍스트 적용과 종료를 안전하게 처리한다.

`with_config()`의 `run_name`, `tags`, `metadata`는 LangSmith 화면에서 실험을 검색하고 비교하기 위한 정보다. 특히 키 이름은 `"tegs"`가 아니라 `"tags"`여야 한다. 같은 질문과 모델을 유지한 채 prompt 조건만 바꾸고, Project나 tag·metadata에 조건 이름과 prompt 버전을 남기면 결과 품질뿐 아니라 token, cost, latency, error도 근거로 비교할 수 있다.

```python
import langsmith as ls

traceable_chain = (recipe_prompt | model | StrOutputParser()).with_config(
    {
        "run_name": "recipe_prompt_v1",
        "tags": ["practice", "prompt_v1"],
        "metadata": {"prompt_version": "v1"},
    }
)

# 이 블록 안의 실행만 My_test Project에 기록한다.
with ls.tracing_context(project_name="My_test", enabled=True):
    answer = traceable_chain.invoke({"ingredient": "토마토와 달걀"})

print(answer)
```

## 다음 학습 방향

다음에는 고객 문의 또는 회의록 예제를 하나 골라 `ChatPromptTemplate → with_structured_output(Pydantic schema) → batch() → DataFrame` 흐름을 직접 실행해 보고 싶다. 입력 데이터와 기대값을 분리한 뒤, prompt 버전만 다른 두 chain을 LangSmith Project에 기록해 출력 형식 준수, 분류 일치, 토큰 사용량, latency를 비교할 것이다. 이렇게 하면 프롬프트를 “잘 썼다”는 감각적 판단에서 벗어나, trace와 평가 결과를 근거로 개선하는 흐름을 연습할 수 있을 것이다.
