# 2026년 8월 10일 회고

오늘은 LangChain의 `Chain`과 LCEL(LangChain Expression Language)을 중심으로, 여러 Runnable을 연결할 때 데이터가 어떤 형태로 흘러가는지 정리했다. 앞에서 살펴본 `Prompt → Model → Parser`가 가장 기본적인 Chain이었다면, 오늘은 순차 실행·조건 분기·병렬 처리·대화 이력까지 붙여 보며 조금 더 애플리케이션다운 흐름을 만들었다. 처음에는 `|` 하나로 연결되는 코드가 단순해 보였지만, 실제로는 이전 단계의 출력 자료형이 다음 단계의 입력이 된다는 점을 계속 확인해야 했다.

## Chain은 입력과 출력을 연결한 실행 경로

LangChain에서 Chain은 하나의 거대한 클래스 이름이라기보다, Prompt·Model·Parser·함수·Retriever처럼 실행 가능한 Runnable을 연결한 처리 경로이다. 각 Runnable은 `invoke()`, `batch()`, `stream()`처럼 공통된 방식으로 실행할 수 있다.

- `invoke(input)`: 입력 하나를 처리해 결과 하나를 반환한다.
- `batch([input1, input2, ...])`: 여러 독립 입력을 처리해 같은 순서의 결과 목록을 반환한다.
- `stream(input)`: 결과를 iterator로 받아 chunk 단위로 소비한다.

특히 `batch()`가 곧 API 요청 한 번을 뜻하는 것은 아니라는 점을 구분했다. Runnable 구현에 따라 여러 `invoke()`가 병렬로 실행될 수 있고, 모델 공급자의 전용 batch 기능을 쓸 수도 있다. 따라서 여러 입력을 코드에서 편하게 다룰 수 있다는 것과 요청 수·비용이 반드시 줄어든다는 것은 다른 이야기다.

가장 기본적인 Simple Chain은 dict 입력을 PromptValue로 바꾸고, LLM이 `AIMessage`를 반환한 뒤, Parser가 최종 문자열만 꺼내는 흐름이었다.

```python
from langchain_core.output_parsers import StrOutputParser
from langchain_core.prompts import PromptTemplate
from langchain_openai import ChatOpenAI

prompt = PromptTemplate(
    template="{country}의 수도는 어디인가?",
    input_variables=["country"],
)
llm = ChatOpenAI(model=CHAT_MODEL_NAME, use_responses_api=True)
chain = prompt | llm | StrOutputParser()

# dict → PromptValue → AIMessage → str
response = chain.invoke({"country": "뉴질랜드"})
print(response)
```

앞 단계의 결과를 다음 단계에 그대로 전달할 때는 입력 key를 다시 맞춰 줄 필요가 없는지도 확인했다. 번역 Chain의 문자열 출력은 요약 Prompt의 `{text}` 하나를 채우는 입력으로 사용할 수 있으므로, `RunnableSequence` 또는 `|`로 번역과 요약을 하나의 Sequential Chain으로 만들 수 있다. 반대로 중간 결과의 자료형이 다음 단계가 기대하는 형태와 다르면, 변환용 Runnable을 사이에 넣어야 한다.

```python
translation_chain = translation_prompt | llm | StrOutputParser()
summary_chain = summary_prompt | llm | StrOutputParser()

# 원문 str → 번역 결과 str → 요약 결과 str
translate_and_summarize = translation_chain | summary_chain
result = translate_and_summarize.invoke(sentence)
print(result)
```

## LCEL은 Runnable 사이의 데이터 흐름을 읽는 표현이다

LCEL은 `first | second | third`처럼 Runnable을 왼쪽에서 오른쪽으로 연결하는 표현 방식이다. `|`는 단순한 파이프 모양이 아니라, 앞 Runnable의 출력 전체를 뒤 Runnable의 입력으로 전달한다는 뜻이다. 그래서 Chain을 만들기 전에 `입력 → 변환 → 출력 → 다음 입력`을 적어 보는 습관이 중요했다.

`RunnableLambda`는 일반 Python 함수를 Runnable으로 감싸는 방법이다. 함수의 계산 자체는 그대로 두면서 `invoke()`, `batch()`, `stream()`과 LCEL 연결 기능을 사용할 수 있다. 완성된 값 하나를 처리하는 간단한 변환에는 `RunnableLambda`가 잘 맞았다.

```python
from langchain_core.runnables import RunnableLambda

def normalize_order_code(text: str) -> dict:
    code = text.strip().upper()
    return {
        "order_code": code,
        "prefix": code.split("-")[0] if "-" in code else "",
    }

order_code_chain = RunnableLambda(normalize_order_code)

print(order_code_chain.invoke("  skn-42  "))
print(order_code_chain.batch([" skn-7 ", " lab-12 "]))
```

스트리밍도 조금 더 구분해서 봤다. `RunnableLambda(lambda text: text.upper())`는 완성된 문자열을 받은 뒤 대문자 문자열 하나를 반환하므로, `stream()`으로 읽어도 기본적으로 최종 결과 하나가 나온다. 반면 callable이 generator이고 `yield`로 문자 하나씩 반환하면 여러 chunk를 만들 수 있다. 앞 단계에서 도착하는 chunk를 기다리지 않고 곧바로 변환해야 할 때는 `RunnableGenerator`를 사용한다.

```python
from collections.abc import Iterator
from langchain_core.runnables import RunnableGenerator, RunnableLambda

def generate_characters(text: str) -> Iterator[str]:
    for char in text:
        yield char

def wrap_non_space_chunks(chunks: Iterator[str]) -> Iterator[str]:
    for chunk in chunks:
        if chunk != " ":
            yield f"<{chunk}>"

character_source = RunnableLambda(generate_characters)
non_space_chain = character_source | RunnableGenerator(wrap_non_space_chunks)

print(list(non_space_chain.stream("A B")))
# ['<A>', '<B>']
```

여기서 generator는 값을 한 번씩 소비한다는 점도 다시 확인했다. `next()`로 첫 값을 꺼내면 같은 generator를 `list()`로 바꿀 때 첫 값은 다시 나오지 않는다. stream 결과를 디버깅할 때도 iterator를 한 번 소비한 뒤 빈 결과가 나온다면, 코드가 잘못된 것이 아니라 이미 읽은 객체를 재사용한 것은 아닌지 먼저 확인해야 한다.

## 순차, 병렬, 원본 보존의 역할을 나누기

`RunnableSequence`는 앞 단계의 출력이 다음 단계 입력이 되는 순차 연결이다. 예를 들어 공백을 제거한 문자열을 Unicode 코드 포인트 목록으로 바꾸는 Chain은 `str → str → list[int]` 흐름으로 읽을 수 있었다. 같은 Runnable을 사용해도 순서를 바꾸면 중간값의 자료형과 최종 결과가 달라질 수 있으므로, `|`의 순서를 임의로 바꾸면 안 된다.

반면 `RunnableParallel`은 같은 원본 입력을 여러 Runnable에 전달하고 이름이 있는 dict로 묶는다. 하나의 입력에서 제곱·팩토리얼·짝수 여부처럼 서로 독립적인 특징을 함께 계산할 때 사용할 수 있다. 원본도 이후 단계에서 필요하다면 `RunnablePassthrough()`로 변경하지 않고 보존한다.

```python
from langchain_core.runnables import (
    RunnableLambda,
    RunnableParallel,
    RunnablePassthrough,
)

order_features = RunnableParallel(
    original=RunnablePassthrough(),
    subtotal=RunnableLambda(
        lambda order: order["unit_price"] * order["quantity"]
    ),
    shipping_fee=RunnableLambda(
        lambda order: 1000 if order["member"] else 0
    ),
)

features = order_features.invoke(
    {"unit_price": 12000, "quantity": 3, "member": True}
)
print(features)
```

이 구조에서는 `original`, `subtotal`, `shipping_fee`가 한 dict에 함께 남는다. 이후 Runnable에서 소계와 배송비를 합쳐 결제 금액을 만들거나, 원본 주문의 다른 필드를 다시 참조할 수 있다. 즉 Parallel은 여러 값을 동시에 계산하는 것만이 아니라, 다음 단계에서 사용할 데이터 구조를 만드는 방법이기도 했다.

## 조건에 따라 다른 Chain으로 보내기

모든 질문을 같은 프롬프트로 처리할 필요는 없다. `RunnableBranch`를 사용하면 입력을 검사해 채점 요청은 채점 Chain으로, 숫자가 포함된 질문은 수학 Chain으로, 나머지는 일반 챗봇 Chain으로 보낼 수 있었다. Branch는 위에서부터 조건을 확인하므로, 더 구체적인 조건을 앞에 두고 마지막에는 기본 Chain을 둬야 한다.

```python
from langchain_core.runnables import RunnableBranch, RunnablePassthrough

def is_grading_request(input_dict: dict) -> bool:
    return input_dict.get("text", "").strip().startswith("채점")

def has_number(input_dict: dict) -> bool:
    return any(char.isdigit() for char in input_dict.get("text", ""))

routing_chain = RunnableBranch(
    (is_grading_request, grading_chain),
    (has_number, math_chain),
    default_chain,
)

# 문자열 입력을 Branch가 기대하는 {"text": ...}로 바꾼다.
full_chain = {"text": RunnablePassthrough()} | routing_chain
print(full_chain.invoke("4^2^2 결과는?"))
```

여기서 `RunnablePassthrough`는 계산을 하지 않는 쓸모없는 단계가 아니라, 문자열 하나를 `{"text": 문자열}` 형태로 포장해 Branch의 입력 계약에 맞추는 역할을 한다. 조건 함수가 `input_dict["text"]`를 읽도록 만들었다면, Chain에 들어가는 값도 그 구조를 따라야 한다.

## 대화 이력은 session_id별로 분리해서 관리하기

마지막으로 `RunnableWithMessageHistory`로 이전 대화를 프롬프트에 넣고, 새 사용자 메시지와 모델 응답을 다시 저장하는 흐름을 살펴봤다. `MessagesPlaceholder("history")`는 이전 메시지를 모델 입력의 중간에 넣는 자리이고, `session_id`는 어떤 대화 이력을 꺼내고 저장할지 정하는 키다.

```python
from langchain_core.chat_history import InMemoryChatMessageHistory
from langchain_core.prompts import ChatPromptTemplate, MessagesPlaceholder
from langchain_core.runnables.history import RunnableWithMessageHistory

store: dict[str, InMemoryChatMessageHistory] = {}

def get_session_history(session_id: str) -> InMemoryChatMessageHistory:
    if session_id not in store:
        store[session_id] = InMemoryChatMessageHistory()
    return store[session_id]

prompt = ChatPromptTemplate.from_messages([
    ("system", "당신은 {domain} 분야의 조언자이다."),
    MessagesPlaceholder(variable_name="history"),
    ("human", "{question}"),
])

chain_with_history = RunnableWithMessageHistory(
    prompt | llm,
    get_session_history,
    input_messages_key="question",
    history_messages_key="history",
)
```

같은 `session_id`를 사용하면 이전에 말한 이름이나 맥락을 다음 질문에서 활용할 수 있고, 다른 `session_id`를 사용하면 이력이 섞이지 않는다. 다만 이번 예제의 `InMemoryChatMessageHistory`는 Python 프로세스 안에서만 유지된다. 프로그램을 다시 시작하거나 서버 인스턴스가 바뀌면 사라지므로, 실제 서비스에서는 Redis나 데이터베이스처럼 영속 저장소와 보존 기간·삭제 정책을 함께 설계해야 한다.

오늘 학습을 통해 LCEL은 LangChain 문법 하나를 외우는 것보다, 여러 단계의 입력과 출력 계약을 눈에 보이게 만드는 방식이라는 점을 알게 되었다. 다음에는 Retriever까지 포함한 RAG Chain을 `RunnableParallel`과 `RunnablePassthrough`로 구성해 질문 원문과 검색 문맥을 함께 보존하고, 검색 결과가 없거나 조건이 맞지 않는 경우도 테스트해 보고 싶다.
