# 2026년 8월 1주차 통합 회고

> 기간: 2026년 8월 4일 ~ 8월 7일  
> 작성 기준: 일일 회고록과 프로젝트 노트북 소스코드

## 한 주 요약

이번 주에는 OpenAI API의 기본 호출에서 출발해 임베딩 검색, Moderation, Function Calling, LangChain, LangSmith, Retrieval, RAG까지 LLM 애플리케이션의 주요 구성 요소를 순서대로 학습했다. 프로젝트 코드를 다시 확인해 보니, 개념을 읽는 데 그치지 않고 실제로 **입력 구성 → 모델 호출 → 검색 또는 도구 실행 → 구조화된 결과 처리 → 실행 추적**의 흐름을 각각 구현해 보았다는 점이 가장 큰 성과였다.

특히 이제는 LLM이 모든 일을 하는 하나의 블랙박스가 아니라, 프롬프트·모델·파서·검색기·벡터 저장소·외부 함수·관찰 도구가 연결된 시스템이라는 관점으로 이해하게 되었다. 다음 단계에서는 각 노트북의 실습 코드를 하나의 작은 서비스 흐름으로 연결하고, 테스트 데이터와 trace를 근거로 품질을 비교·개선하는 경험이 필요하다.

## 프로젝트 확인 범위

| 학습 주제 | 확인한 소스 |
| --- | --- |
| 대화, 임베딩, Moderation, Function Calling | [`03_openai_api`](./03_openai_api) |
| SFT 데이터 설계·준비 | [`04_stf_data_preparation`](./04_stf_data_preparation) |
| LangChain·LangSmith | [`05_langchain/01_langchain_overview`](./05_langchain/01_langchain_overview) |
| Model I/O, 구조화된 출력, Retrieval·RAG | [`05_langchain/02_lagn_chain_component`](./05_langchain/02_lagn_chain_component) |

## 주요 학습 성과와 코드 근거

### 1. 임베딩으로 의미 기반 검색을 구현했다

[`04_embeddings.ipynb`](./03_openai_api/04_embeddings.ipynb)에서는 리뷰 텍스트를 `text-embedding-3-small` 벡터로 바꾸고, 질문 벡터와 각 리뷰 벡터의 코사인 유사도를 계산해 상위 문서를 찾았다. 텍스트를 벡터로 바꾸는 과정과 검색 점수 계산을 분리해 작성한 점이 좋았다.

```python
def texts_to_embedding(texts, model="text-embedding-3-small"):
    if not texts:
        return []

    for i, text in enumerate(texts):
        if not isinstance(text, str) or not text.strip():
            raise ValueError(f"{i}번째 텍스트가 비어 있거나 문자열이 아닙니다: {text!r}")

    normalized_texts = [text.replace("\n", " ") for text in texts]
    response = client.embeddings.create(model=model, input=normalized_texts)
    return [item.embedding for item in response.data]


def review_search(query, embed_df, top_n=5):
    query_embedding = texts_to_embedding([query])
    scored = embed_df.copy()
    scored["cos_sim"] = scored["embedding"].apply(
        lambda x: cosine_similarity(query_embedding, [x])[0, 0]
    )
    return scored.sort_values("cos_sim", ascending=False).head(top_n)
```

이 구현을 통해 임베딩 차원, 입력 순서, 정규화와 유사도 점수의 의미를 확인했다. 다만 유사도 점수가 높다고 해서 항상 질문에 적합한 근거는 아니므로, 다음에는 테스트 질문·기대 문서·검색 품질 지표를 따로 두고 검증해야 한다.

### 2. 안전성 검사와 외부 도구 호출의 책임을 구분했다

[`05_moderation.ipynb`](./03_openai_api/05_moderation.ipynb)에서는 전체 판정(`flagged`)과 범주별 점수를 DataFrame으로 정리해, Moderation 결과를 차단의 정답이 아니라 사람의 검토 또는 정책 엔진에 전달할 신호로 다뤘다. 사용자 입력뿐 아니라 이미지 입력과 Responses API의 입력·출력 moderation도 함께 실습했다.

[`06_function_calling.ipynb`](./03_openai_api/06_function_calling.ipynb)에서는 날씨 조회 함수를 모델에 등록하고, 모델이 만든 호출 요청을 애플리케이션에서 검증·실행한 뒤 그 결과를 다시 모델에 전달했다. 아래 코드는 Function Calling의 핵심인 "모델의 요청"과 "애플리케이션의 실행"을 분리한 부분이다.

```python
def run_conversation(prompt):
    messages = [
        {"role": "system", "content": "너는 도구 결과를 근거로 답하는 챗봇이다"},
        {"role": "user", "content": prompt},
    ]
    response = client.chat.completions.create(
        model="gpt-5.6-luna",
        messages=messages,
        tools=tools,
        reasoning_effort="none",
        tool_choice="auto",
    )

    response_message = response.choices[0].message
    tool_calls = response_message.tool_calls or []
    if not tool_calls:
        return response_message.content or ""

    available_functions = {"get_current_weather": get_current_weather}
    messages.append(response_message)

    for tool_call in tool_calls:
        function_name = tool_call.function.name
        if function_name not in available_functions:
            raise ValueError(f"허용하지 않은 함수 호출이다: {function_name}")

        function_args = json.loads(tool_call.function.arguments)
        function_response = available_functions[function_name](**function_args)
        messages.append({
            "role": "tool",
            "tool_call_id": tool_call.id,
            "content": function_response,
        })

    final_response = client.chat.completions.create(
        model="gpt-5.6-luna", messages=messages, tools=tools,
        reasoning_effort="none",
    )
    return final_response.choices[0].message.content or ""
```

이 과정에서 모델이 만든 함수명과 인자를 무조건 신뢰하면 안 되며, 허용 목록·스키마·권한을 서버 측에서 검증해야 한다는 점을 배웠다.

### 3. LangChain으로 입력·출력 계약을 만들고, 여러 결과를 평가했다

[`01_model_io.ipynb`](./05_langchain/02_lagn_chain_component/01_model_io.ipynb)에서 `Prompt → Model → Parser` 구조, `invoke()`·`batch()`·`stream()`의 차이, few-shot prompt, Pydantic 기반 구조화된 출력을 실습했다. 특히 고객 문의 예제는 모델 출력의 형태를 명시하고 여러 입력을 병렬 처리한 뒤 기대값과 비교한다는 점에서, 단순한 호출 예제보다 한 단계 더 실제 업무에 가까운 구조였다.

```python
class TicketAnalysis(BaseModel):
    ticket_id: str = Field(description="입력으로 받은 문의 id")
    category: Literal["결제", "배송", "계정", "반품", "기타"]
    urgency: Literal["낮음", "보통", "높음"]
    summary: str
    required_information: list[str]
    reply_draft: str


ticket_schema_model = model.with_structured_output(TicketAnalysis)
ticket_chain = ticket_prompt | ticket_schema_model

ticket_inputs = [
    {"ticket_id": case["ticket_id"], "text": case["text"]}
    for case in ticket_cases
]
ticket_results = ticket_chain.batch(ticket_inputs, max_concurrency=3)

ticket_frame = pd.DataFrame([result.model_dump() for result in ticket_results])
ticket_frame["category_match"] = (
    ticket_frame["category"] == ticket_frame["expected_category"]
)
ticket_frame["urgency_match"] = (
    ticket_frame["urgency"] == ticket_frame["expected_urgency"]
)
```

schema는 결과의 필드와 자료형을 보장하는 데이터 계약이지, 내용의 사실성을 보장하지는 않는다. 따라서 이처럼 기대값을 분리해 비교하는 방식이 중요하며, 다음에는 일치율과 실패 사례까지 기록해 프롬프트 개선 근거로 활용해야 한다.

### 4. LangSmith로 프롬프트 실험을 추적할 기반을 만들었다

[`02_langsmith.ipynb`](./05_langchain/01_langchain_overview/02_langsmith.ipynb)에서는 chain에 실행 이름·태그·메타데이터를 부여하고, 특정 Project에 trace를 남기는 코드를 작성했다.

```python
chain_v1 = (prompt_v1 | model | parser_v1).with_config(
    {
        "run_name": "langsmith_trace_practice",
        "tags": ["lecture", "prompt_v1"],
        "metadata": {"prompt_version": "v1"},
    }
)

with ls.tracing_context(project_name=project_name, enabled=True):
    answer = chain_v1.invoke({"question": question})
```

이를 통해 한 요청 전체인 trace와 그 안의 Prompt·Model·Parser 같은 run을 구분하고, 응답 품질뿐 아니라 모델 호출의 token, latency, error를 근거로 살펴볼 수 있게 되었다.

### 5. 검색 근거를 답변에 연결하는 RAG를 구현했다

[`02_retriever.ipynb`](./05_langchain/02_lagn_chain_component/02_retriever.ipynb)에서는 웹페이지와 PDF를 `Document`로 변환하고, chunk 분할·임베딩·Chroma 저장·Retriever 검색을 거쳐 RAG 답변을 만드는 파이프라인을 작성했다. 아래는 검색된 문서의 출처와 페이지를 문맥으로 만들고, 답변과 검색 결과를 함께 반환하는 구현이다.

```python
db_retriever = reloaded_db.as_retriever(
    search_type="similarity", search_kwargs={"k": 3}
)

rag_prompt = ChatPromptTemplate.from_messages([
    ("system", "검색 문맥만 근거로 한국어로 답한다. 문맥에 답이 없으면 모른다고 답한다. "
               "답변 마지막에 사용한 출처를 적는다.\n\n[검색 문맥]\n{context}"),
    ("human", "{question}"),
])
rag_chain = rag_prompt | llm | StrOutputParser()

def run_retrieval_rag(question: str) -> dict:
    documents: list[Document] = db_retriever.invoke(question)
    context_parts = []
    for document in documents:
        source = document.metadata.get("source", "unknown")
        page = document.metadata.get("page", "web")
        context_parts.append(
            f"[출처: {source}, 페이지: {page}]\n{document.page_content}"
        )

    answer = rag_chain.invoke({
        "context": "\n\n---\n\n".join(context_parts),
        "question": question,
    })
    return {"documents": documents, "answer": answer}
```

RAG는 LLM 앞에 검색을 한 번 붙이는 기능이 아니라, 문서 수집·chunking·임베딩 모델·검색 개수·문맥 구성·출처 표기가 모두 연결된 구조임을 확인했다. 특히 반환값에 마지막 `document`가 아닌 전체 `documents`를 담아, 검색 근거를 답변과 함께 점검할 수 있게 작성한 점이 중요했다.

## 마무리

이번 주의 성과는 개별 API와 프레임워크 기능을 각각 실행해 본 것에 더해, 이들이 하나의 LLM 애플리케이션 안에서 어떤 역할을 맡는지 이해한 데 있다. 다음 주에는 "동작하는 예제"를 "평가 가능하고 신뢰할 수 있는 작은 애플리케이션"으로 발전시키는 데 집중하겠다.
