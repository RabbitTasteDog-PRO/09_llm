# 2026년 8월 12일 회고

오늘은 어제 만든 기본 RAG 흐름을 실제 예제로 확장해, 음식 이미지에서 와인을 추천하는 `AI Sommelier RAG`를 구성했다. 단순히 "이 음식에 어울리는 와인을 알려 줘"라고 모델에 바로 묻는 방식과, Wine Magazine 리뷰를 먼저 찾아 근거로 사용하게 하는 RAG 방식을 나누어 보았다.

처음에는 와인 추천이라는 결과만 보면 모델 하나와 Prompt 하나면 될 것 같았다. 하지만 이미지가 들어오면 그 이미지를 검색에 바로 넣을 수 없고, Pinecone에 저장된 텍스트 리뷰와 비교할 수 있는 영어 풍미 문장으로 바꿔야 했다. 오늘은 이 중간 단계를 하나씩 연결하면서, RAG는 검색만 붙이는 기능이 아니라 입력을 검색 가능한 형태로 바꾸는 과정까지 포함한다는 점을 확인했다.

## 오늘 처음 사용한 Pinecone은 무엇인가

Pinecone은 텍스트를 숫자 배열인 embedding vector로 저장하고, 의미가 비슷한 vector를 빠르게 찾는 관리형 Vector DB이다. 일반 데이터베이스가 `와인 이름이 Cabernet인 행`처럼 정확한 값이나 조건을 찾는 데 강하다면, Pinecone은 `로즈마리를 곁들인 구운 소고기`처럼 문장이 비슷한 의미의 리뷰를 찾는 데 사용한다.

Pinecone이 LLM 답변을 만들어 주는 것은 아니다. 문서와 질문을 같은 Embedding Model로 vector로 바꾸고, 질문 vector와 가까운 문서 vector를 찾아 반환한다. 그 반환값인 `list[Document]`를 LLM Prompt에 넣어 답변을 만들게 하는 것이 이 프로젝트의 RAG 흐름이다.

```text
문서 본문 → Embedding Model → 문서 vector → Pinecone
사용자 query → 같은 Embedding Model → query vector ─┘
                                             ↓
                                    가까운 Document 검색
```

Pinecone Console에서 먼저 `index`를 만들고 API Key를 발급한 뒤 Python에서 연결한다. 여기서 `index`는 같은 vector 차원과 유사도 기준을 가진 검색 공간이고, `namespace`는 하나의 index 안에서 데이터를 논리적으로 나누는 구획이다. 이 실습에서는 Wine Magazine 리뷰를 `winemag-review-data` index의 기본 namespace에 저장했다.

또 `text-embedding-3-small`을 기본 설정으로 사용하면 vector 차원이 `1536`이므로, Pinecone index도 `Dense`, dimension `1536`, metric `cosine`으로 맞춰야 한다. 문서 저장 때 사용한 Embedding Model과 검색 query에 사용한 Embedding Model이 달라도 검색 품질이 무너지거나 dimension 오류가 날 수 있다.

```python
from langchain_openai import OpenAIEmbeddings
from langchain_pinecone import PineconeVectorStore

embeddings = OpenAIEmbeddings(model="text-embedding-3-small")

wine_vector_store = PineconeVectorStore(
    index_name="winemag-review-data",
    embedding=embeddings,
)
```

API Key는 코드에 직접 적지 않고 `.env`에서 읽는다. Pinecone은 클라우드 서비스이므로 index 생성, 문서 적재, 검색 모두 계정과 네트워크 연결이 필요하다. 반면 어제 사용한 Chroma는 로컬 PC에서 바로 시작하기 좋은 Vector Store이고, Pinecone은 다른 실행 환경에서도 같은 index를 공유할 수 있다는 차이가 있었다.

## Wine Magazine 리뷰를 Pinecone에 저장하는 인덱싱

이번 예제의 데이터는 Wine Magazine 리뷰 CSV이다. CSV의 한 행을 LangChain `Document` 하나로 만들고, `page_content`를 OpenAI Embedding으로 vector로 바꾼 뒤 Pinecone에 저장한다.

```text
Wine Magazine CSV 행
→ Document
→ embedding vector
→ Pinecone index
→ Retriever
```

여기서 `Document`는 단순 문자열이 아니라 검색 본문과 출처 정보를 함께 담는 객체이다. `page_content`에는 실제 리뷰 내용이 들어가고, `metadata`에는 CSV 파일 경로와 행 번호처럼 원본을 확인할 정보를 둔다. 나중에 검색된 결과를 봤을 때 어떤 리뷰에서 왔는지 확인하려면 이 metadata도 중요하다.

```python
from langchain_community.document_loaders import CSVLoader

loader = CSVLoader(
    file_path="./winemag-data-130k-v2.csv",
    encoding="utf-8",
)

# CSV 한 행 → Document 하나
wine_docs = loader.load()
```

전체 리뷰는 한 번에 저장하지 않고, 먼저 Pinecone index와 연결한 뒤 100개씩 나누어 `add_documents()`로 추가했다. 이 호출에서 각 `Document.page_content`가 embedding되고, vector와 원문·metadata가 함께 원격 Pinecone index에 저장된다.

```python
from langchain_openai import OpenAIEmbeddings
from langchain_pinecone import PineconeVectorStore

embeddings = OpenAIEmbeddings(
    model="text-embedding-3-small",
)

wine_vector_store = PineconeVectorStore.from_documents(
    documents=[],
    embedding=embeddings,
    index_name="winemag-review-data",
)

for start in range(0, len(wine_docs), 100):
    wine_vector_store.add_documents(wine_docs[start:start + 100])
```

여기서 조심할 점도 분명했다. `text-embedding-3-small`을 기본 설정으로 쓰면 vector 차원은 `1536`이므로, Pinecone index도 같은 차원으로 만들어야 한다. 차원이 다르면 저장과 검색이 되지 않는다. 또 전체 CSV는 약 13만 행이라 embedding API 비용과 저장 시간이 든다. 같은 코드를 무심코 다시 실행하면 문서가 중복으로 들어갈 수 있으니, 적재 전후에 index 통계와 검색 결과를 확인해야 한다.

## 풍미 문장은 RAG 검색 query를 만드는 전처리 결과이다

RAG에서 query는 반드시 사용자가 입력한 질문 원문만 뜻하지 않는다. Retriever가 관련 문서를 잘 찾을 수 있도록, 사용자의 입력을 문서와 비교하기 좋은 검색어로 정리한 결과도 query가 된다. 텍스트 질문은 그대로 query로 쓸 수 있고, 필요하면 질문을 더 구체적으로 다시 쓰거나 검색 조건을 추가할 수 있다. 이미지 입력은 텍스트 리뷰와 직접 의미 비교할 수 없으므로, 먼저 Vision 모델이 이미지 내용을 텍스트로 바꾸는 전처리 과정이 필요했다.

Pinecone index에는 이미지가 아니라 영어 와인 리뷰 텍스트가 들어 있다. 그래서 음식 이미지를 바로 Retriever에 넣는 대신, Vision 가능한 모델로 핵심 재료·조리법·맛을 영어 한 문장으로 설명하게 했다. 이 풍미 문장이 Wine Magazine 리뷰를 찾는 retrieval query가 된다.

```text
음식 이미지 URL
→ Vision LLM
→ 영어 풍미 문장
→ Query embedding
→ 유사한 와인 리뷰 5개
```

검색 query에는 답변 자체보다 문서를 고르는 데 필요한 특징을 담아야 한다. 이 예제에서는 `소고기`, `로즈마리`, `구운 채소`, `육즙`, `구이`처럼 재료·조리법·향·맛·식감을 함께 넣는다. `맛있는 음식에 어울리는 와인`처럼 너무 넓은 문장은 관련 없는 리뷰까지 찾을 수 있고, 특정 와인 이름을 미리 넣으면 검색 결과를 한쪽으로 치우치게 할 수 있다. 문서가 영어 리뷰이기 때문에 영어 query를 만들었지만, 핵심은 언어 자체보다 문서의 표현과 비교할 수 있는 구체적인 의미를 담는 것이다.

```text
사용자 입력
├─ 텍스트 질문 → 필요하면 검색 목적에 맞게 정리 → retrieval query
└─ 음식 이미지 → Vision LLM으로 재료·조리법·풍미를 문장화 → retrieval query
                                                          ↓
                                                   Retriever 검색
```

`describe_dish_flavor()`는 바로 결과 문자열을 만드는 함수처럼 보이지만, 실제로는 `Prompt → Model → Parser`로 된 Runnable을 반환한다. `RunnableLambda`가 이 일반 함수를 LCEL에서 실행할 수 있게 감싼다.

```python
from langchain_core.runnables import RunnableLambda


def describe_dish_flavor(payload: dict):
    image_contents = [
        {"image_url": image_url}
        for image_url in payload["image_urls"]
    ]
    prompt = dish_analysis_prompt + HumanMessagePromptTemplate.from_template(
        image_contents
    )
    return prompt | chat_model | StrOutputParser()


describe_dish_flavor_chain = RunnableLambda(describe_dish_flavor)
```

처음에는 이미지 분석 결과를 한국어와 영어로 둘 다 받으려 했지만, 검색 단계에서 필요한 것은 Wine Magazine 영어 리뷰와 비교할 영어 query라는 점을 다시 확인했다. 출력 형식을 영어 한 문장으로 제한하면 query의 목적이 더 분명해지고, query·검색 결과·최종 한국어 추천을 각각 분리해서 확인하기 쉬워진다. 즉, 풍미 문장은 사용자에게 보여 주기 위한 최종 설명이 아니라 Retriever가 어떤 리뷰를 찾아야 하는지 알려 주는 중간 입력값이다.

## Retriever는 답변하지 않고 리뷰만 찾는다

풍미 문장을 얻은 뒤에는 `PineconeVectorStore`를 Retriever로 바꿔 상위 5개 리뷰를 찾았다. Retriever의 입력은 `str`, 출력은 `list[Document]`이다. 와인을 추천하는 답변은 아직 만들지 않는다.

```python
wine_retriever = wine_vector_store.as_retriever(
    search_type="similarity",
    search_kwargs={"k": 5},
)

retrieved_documents = wine_retriever.invoke(
    "A succulent roast beef with rosemary and roasted vegetables."
)
```

검색 결과의 `Document` 목록은 LLM이 바로 읽기 편한 형태가 아니어서, 각 리뷰의 `page_content`를 하나의 문자열로 합쳤다. 이때 원래 풍미 query도 같이 보존해야, 최종 모델이 "무슨 음식에 대한 추천인지"와 "어떤 리뷰를 근거로 해야 하는지"를 모두 알 수 있다.

```python
from langchain_core.documents import Document


def format_documents(documents: list[Document]) -> str:
    return "\n\n--- review ---\n\n".join(
        document.page_content for document in documents
    )


def build_retrieval_context(payload: dict) -> dict:
    return {
        "dish_flavor": payload["dish_flavor"],
        "wine_reviews": format_documents(payload["retrieved_documents"]),
    }
```

이 부분을 보면서 `RunnableParallel`의 역할도 조금 더 이해했다. 하나의 풍미 문자열을 한 갈래에서는 그대로 `dish_flavor`에 남기고, 다른 갈래에서는 Retriever에 전달해 `retrieved_documents`를 만든다. 그 뒤 두 값을 하나의 `dict`로 합쳐 Generation 단계에 넘긴다.

```text
풍미 문자열
├─ 그대로 보존 → dish_flavor
└─ Retriever 검색 → retrieved_documents
                    ↓
       {dish_flavor, wine_reviews}
```

## 검색된 리뷰만 근거로 추천하도록 제한하기

최종 추천 Prompt에는 요리 설명과 검색 리뷰를 명확히 나누어 넣었다. 특히 `wine_reviews` 안의 텍스트는 데이터이지 지시문이 아니라고 system prompt에 적었다. 외부에서 가져온 문서에 이상한 명령이 섞이더라도 모델이 그 명령을 따르지 않게 하기 위한 기본적인 방어이다.

```python
WINE_RECOMMENDATION_HUMAN_PROMPT = """다음 요리 설명과 와인 리뷰를 사용해 와인 페어링을 추천한다.

<dish_flavor>
{dish_flavor}
</dish_flavor>

<wine_reviews>
{wine_reviews}
</wine_reviews>

추천 와인과 이유:"""
```

Prompt에는 제공된 리뷰에 없는 와인 이름이나 사실을 새로 만들지 말고, 근거가 부족하면 부족하다고 답하도록 정리했다. RAG라고 해서 환각이 자동으로 사라지는 것은 아니기 때문에, 검색 결과를 답변 근거로 제한하는 지시와 실제 검색 결과 확인을 함께 해야 한다.

## Streamlit 앱으로 연결하면서 확인한 입력 검증

Notebook에서 끝나지 않고 `ai_sommelier_rag_app` 폴더에 Streamlit 화면도 만들었다. 사용자가 음식 이미지 URL을 입력하면 URL을 먼저 검사하고, 이미지 분석·Pinecone 검색·추천 생성 순서로 실행한다. 최종 텍스트는 `stream()`으로 조금씩 받아 `st.write_stream()`에 전달한다.

```python
def ai_sommelier_rag(*image_urls: str):
    chain = build_ai_sommelier_chain()
    return chain.stream({"image_urls": list(image_urls)})


with st.spinner("음식과 관련된 와인 리뷰를 분석중입니다..."):
    st.write_stream(ai_sommelier_rag(image_url))
```

이미지 URL은 아무 주소나 받지 않도록 `https`인지, `localhost`·사설 IP·loopback IP가 아닌지 먼저 검사했다. 잘못된 입력은 모델이나 외부 서비스에 보내기 전에 막는 것이 좋다. API Key도 오류 메시지나 화면에 직접 표시하지 않고, 누락된 환경 변수 이름만 알려 주도록 했다.

```python
def validate_runtime_settings() -> None:
    required_names = ("OPENAI_API_KEY", "PINECONE_API_KEY")
    missing_names = [name for name in required_names if not os.getenv(name)]

    if missing_names:
        raise SommelierConfigurationError(
            ".env에 필요한 환경 변수를 설정한다: " + ", ".join(missing_names)
        )
```

오늘은 모델에게 바로 추천을 받는 것보다, `이미지 → 풍미 query → Retriever → 리뷰 → 추천`처럼 각 단계가 어떤 값을 받고 무엇을 내보내는지를 나눠 보는 시간이 더 중요했다. 특히 RAG에서 Retrieval과 Generation을 섞어 생각하면 검색이 잘못된 것인지, Prompt가 부족한 것인지 구분하기 어려워진다. 단계별 입력과 출력을 출력해 보고 확인하는 습관을 계속 가져가야겠다.

다음에는 다음을 직접 확인해 보고 싶다.

- 같은 음식에 대해 Baseline 추천과 RAG 추천이 실제로 어떻게 달라지는지 비교하기
- 검색된 5개 리뷰의 `metadata['row']`와 원문을 화면에도 표시해 추천 근거를 확인하기
- 음식이 아닌 이미지와 사설 IP URL이 API 요청 전에 제대로 차단되는지 테스트하기
- Pinecone의 index 이름, namespace, embedding 차원이 맞지 않을 때 어떤 오류가 나는지 확인하기
- 13만 개 리뷰를 다시 적재하지 않도록 문서 ID와 적재 상태를 관리하는 방법 찾아보기

오늘 배운 내용을 한 문장으로 정리하면, 이미지 기반 RAG 추천은 모델에게 답을 바로 부탁하는 기능이 아니라, 이미지를 텍스트 검색어로 바꾸고 찾은 문서를 근거로 다음 모델 호출을 설계하는 흐름이라는 점이다.
