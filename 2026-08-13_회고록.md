# 2026년 8월 13일 회고

오늘은 RAG에서 답변 모델보다 먼저 살펴봐야 하는 검색 단계를 조금 더 깊게 정리했다. 어제까지는 `문서를 vector로 저장하고 비슷한 문서를 찾아 Prompt에 넣는다`는 기본 흐름을 중심으로 봤다면, 오늘은 같은 질문이라도 어떤 검색기를 쓰고, 검색 결과를 어떤 기준으로 섞고, metadata 조건을 어디에서 적용하느냐에 따라 결과가 달라진다는 점을 확인했다.

처음에는 검색 최적화라고 하면 더 좋은 embedding model이나 더 큰 Vector DB를 쓰는 일이라고 생각했다. 하지만 실습을 따라가면서 검색은 하나의 방법으로 끝나지 않는다는 것을 알게 됐다. 단어가 정확히 겹칠 때 강한 BM25, 문장의 의미가 비슷할 때 강한 Dense Retrieval, 두 결과를 순위로 합치는 RRF, 그리고 저자나 카테고리처럼 의미 유사도만으로 해결할 수 없는 조건을 다루는 Metadata Filtering과 Self-query까지 연결해 보았다.

오늘의 전체 흐름은 아래처럼 정리할 수 있다.

```text
문서 CSV
├─ 본문 → embedding → Pinecone index
├─ 본문 토큰 → BM25 index
└─ 본문 + author/category → metadata 포함 Pinecone index

사용자 질문
├─ BM25 → 단어 점수 기반 순위
├─ Dense → embedding 유사도 기반 순위
├─ RRF → 두 순위를 결합한 순위
└─ Self-query → 의미 검색어 + metadata filter → Pinecone 검색
```

## 먼저 문서를 다시 넣지 않는 Indexing을 만들었다

검색을 비교하려면 같은 문서 집합을 안정적으로 사용할 수 있어야 한다. `documents.csv`를 읽어 `Document`로 만들고, `text-embedding-3-small`으로 embedding한 뒤 Pinecone의 `adv-rag` index에 저장했다. 이때 index의 vector 차원도 embedding model과 같은 `1536`으로 맞췄다.

중요했던 부분은 문서마다 고정된 `doc_id`를 Pinecone ID로 전달했다는 점이다. 매번 실행할 때 자동 ID를 쓰면 같은 문서가 중복으로 쌓일 수 있다. 반대로 `D1`, `D2`처럼 정해진 ID로 `add_documents()`를 호출하면, 해당 ID가 없을 때는 추가하고 이미 있으면 갱신하는 upsert 흐름으로 관리할 수 있다.

```python
from langchain_core.documents import Document
from langchain_openai import OpenAIEmbeddings
from langchain_pinecone import PineconeVectorStore

embeddings = OpenAIEmbeddings(
    model="text-embedding-3-small",
    dimensions=1536,
)

vector_store = PineconeVectorStore(
    index_name="adv-rag",
    embedding=embeddings,
    namespace="",
)

documents = [
    Document(
        page_content=str(row["content"]),
        metadata={"doc_id": str(row["doc_id"])},
    )
    for _, row in documents_df.iterrows()
]

document_ids = [document.metadata["doc_id"] for document in documents]
vector_store.add_documents(documents=documents, ids=document_ids)
```

여기서 `Document`의 본문은 embedding 대상이고, `metadata['doc_id']`는 검색 결과를 qrels의 정답 문서와 비교하는 key가 된다. RAG에서 metadata는 화면에 출처를 보여 주기 위한 정보이기도 하지만, 오늘처럼 검색 품질을 평가하거나 필터링할 때도 기준점이 된다.

## BM25와 Dense Retrieval은 잘 찾는 방식이 다르다

BM25는 문서와 질의에 함께 나온 단어를 기반으로 점수를 계산하는 sparse retrieval 방식이다. 오늘 실습에서는 한국어 문장을 형태소 단위로 나눈 뒤 `rank_bm25`에 전달했다. 형태소 분석 없이 단순 공백으로 자르면 조사와 어미 때문에 핵심 단어 비교가 흔들릴 수 있어서 `Okt`를 사용했다.

```python
from konlpy.tag import Okt
from rank_bm25 import BM25Okapi

okt = Okt()
tokenized_corpus = [okt.morphs(content) for content in documents_df["content"]]
bm25 = BM25Okapi(tokenized_corpus)


def bm25_search(query: str, top_k: int = 5) -> list[str]:
    query_tokens = okt.morphs(query)
    scores = bm25.get_scores(query_tokens)
    positive_indices = [
        index for index, score in enumerate(scores) if score > 0
    ]
    ranked_indices = sorted(
        positive_indices,
        key=lambda index: scores[index],
        reverse=True,
    )[:top_k]
    return [documents_df["doc_id"].iloc[index] for index in ranked_indices]
```

반면 Dense Retrieval은 문서와 질의를 같은 embedding model로 vector로 바꾸고, Pinecone에서 의미적으로 가까운 문서를 찾는다. 질문과 문서의 단어가 꼭 일치하지 않아도 의미가 유사하면 검색할 수 있다는 장점이 있다.

```python
dense_documents_and_scores = vector_store.similarity_search_with_score(
    query="전주 비빔밥과 진주 비빔밥의 재료와 맛 차이",
    k=5,
)

dense_doc_ids = [
    document.metadata["doc_id"]
    for document, score in dense_documents_and_scores
]
```

두 방식은 경쟁 관계라기보다 서로 놓치는 부분이 다르다. BM25는 고유명사, 제품명, 기술 용어처럼 질문의 표현이 문서에 그대로 있을 때 강하다. Dense는 같은 뜻을 다른 말로 쓴 질문이나 문서의 문맥을 찾을 때 장점이 있다. 그래서 검색기를 고를 때는 `무엇이 더 최신인가`보다 데이터와 사용자의 질문이 어떤 형태인지 먼저 확인해야 한다.

## 숫자로 평가하니 Dense가 조금 더 좋았지만, RRF가 자동 정답은 아니었다

검색 결과를 눈으로 한두 개만 보면 어떤 방식이 좋은지 판단하기 어렵다. 오늘은 30개 질의의 `relevant_doc_ids`를 qrels로 사용해 `P@5`, `R@5`, `MRR`, `MAP@5`를 계산했다.

- `P@5`: 상위 5개 결과 중 정답 문서가 차지하는 비율
- `R@5`: 전체 정답 문서 중 상위 5개에서 찾은 비율
- `MRR`: 첫 정답 문서가 얼마나 앞 순위에 있는지의 평균
- `MAP@5`: 여러 정답 문서의 순위 품질을 함께 반영한 평균

```python
def compute_metrics(
    predicted_ids: list[str],
    relevant_documents: dict[str, int],
    k: int = 5,
) -> tuple[float, float, float, float]:
    top_k = predicted_ids[:k]
    hit_count = sum(doc_id in relevant_documents for doc_id in top_k)
    precision = hit_count / k
    recall = hit_count / len(relevant_documents) if relevant_documents else 0.0

    reciprocal_rank = next(
        (1 / rank for rank, doc_id in enumerate(top_k, start=1)
         if doc_id in relevant_documents),
        0.0,
    )

    relevant_seen = 0
    precision_sum = 0.0
    for rank, doc_id in enumerate(top_k, start=1):
        if doc_id in relevant_documents:
            relevant_seen += 1
            precision_sum += relevant_seen / rank

    denominator = min(len(relevant_documents), k)
    average_precision = precision_sum / denominator if denominator else 0.0
    return precision, recall, reciprocal_rank, average_precision
```

저장된 실행 결과는 다음과 같았다.

| 지표 | BM25 | Dense | RRF |
| --- | ---: | ---: | ---: |
| P@5 | 0.246667 | 0.260000 | 0.260000 |
| R@5 | 0.883333 | 0.916667 | 0.916667 |
| MRR@5 | 0.966667 | 0.983333 | 0.966667 |
| MAP@5 | 0.849074 | 0.880741 | 0.857963 |

이번 데이터에서는 Dense가 모든 비교 지표에서 가장 높았다. 특히 `MRR@5`가 높다는 것은 첫 관련 문서가 대체로 더 앞쪽에 왔다는 뜻이다. 그러나 이 결과만으로 `Dense가 항상 BM25보다 좋다`고 결론 내릴 수는 없다. 데이터가 30개 질의로 작고, 현재 qrels가 주로 본문 주제와 잘 맞는 형태이기 때문이다.

RRF도 기대와 달리 Dense보다 좋아지지는 않았다. RRF의 목적은 두 검색기의 점수를 같은 기준으로 비교하는 것이 아니라, 각 검색기가 매긴 순위 자체를 결합해 한쪽 검색기가 놓친 문서를 보완하는 것이다. 검색기마다 score의 범위와 의미가 다를 수 있으므로 순위만 쓰는 방식이 안전하다.

```python
def rrf_fuse(
    bm25_ranked_ids: list[str],
    dense_ranked_ids: list[str],
    rank_constant: int = 60,
) -> list[tuple[str, float]]:
    candidate_scores: dict[str, float] = {}

    for ranked_ids in (bm25_ranked_ids, dense_ranked_ids):
        for rank, doc_id in enumerate(ranked_ids, start=1):
            contribution = 1 / (rank_constant + rank)
            candidate_scores[doc_id] = candidate_scores.get(doc_id, 0.0) + contribution

    return sorted(
        candidate_scores.items(),
        key=lambda item: item[1],
        reverse=True,
    )
```

예를 들어 Q2에서는 BM25가 `D13, D2, ...` 순서를, Dense가 `D2, D13, ...` 순서를 반환했고, RRF는 두 결과가 모두 상위에 둔 `D13, D2, ...`를 먼저 배치했다. 이처럼 순위 결합의 결과가 왜 나왔는지 질의별로 확인해야 한다. 이번에는 Dense 결과가 이미 충분히 좋아 RRF로 섞었을 때 첫 정답 순위가 일부 밀렸고, 따라서 MRR과 MAP가 조금 낮아졌다. Hybrid Retrieval을 적용했다는 사실보다 평가 지표가 실제로 개선됐는지가 더 중요하다는 점을 확인했다.

## Metadata Filtering은 검색 대상을 먼저 좁힌다

의미가 비슷한 문서를 찾더라도 사용자가 `역사 또는 기술 카테고리만`, `김민수가 작성한 문서만`처럼 조건을 주면 본문 vector만으로는 충분하지 않다. 그래서 `documents_meta.csv`의 `doc_id`, `author`, `category`를 Document metadata에 넣어 별도의 `adv-rag-meta` index에 저장했다.

```python
metadata = {
    "doc_id": str(row["doc_id"]),
    "author": str(row["author"]),
    "category": str(row["category"]).split(";"),
}

document = Document(
    page_content=str(row["content"]),
    metadata=metadata,
)
```

Pinecone filter는 유사도 순위를 계산하기 전에 조건에 맞지 않는 문서를 후보에서 제외한다. 따라서 답변에 포함돼서는 안 되는 범주를 후처리로 지우는 것보다 안전하고 효율적이다.

```python
category_filter = {"category": {"$in": ["역사", "기술"]}}

results = vector_store.similarity_search(
    query="훈민정음",
    k=5,
    filter=category_filter,
)
```

다만 개발자가 매번 filter 딕셔너리를 직접 만드는 방식은 조건이 정해진 관리자 화면에서는 적합하지만, 일반 사용자의 자연어 질문을 받는 서비스에는 맞지 않는다. 사용자는 Pinecone의 `$in`, `$eq`, `$and` 문법을 알 필요 없이 `김민수가 작성한 검색 관련 문서 2개만 찾아줘`라고 말할 것이기 때문이다.

## Self-query는 자연어를 검색어와 조건으로 나눈다

Self-query Retriever는 질문에서 본문 의미 검색어, metadata filter, 반환 개수를 나눠 `StructuredQuery`로 만드는 방식이다. 이름 때문에 Retriever가 스스로 새로운 질문을 만드는 것처럼 들릴 수 있지만, 오늘 실습의 핵심은 LLM이 자연어를 검색 가능한 구조로 변환한다는 데 있다.

```text
김민수가 작성한 검색 관련 문서 2개만 찾아줘
        ↓
semantic query: 검색 관련
filter: author == 김민수
limit: 2
        ↓
Pinecone filter: {'author': {'$eq': '김민수'}}
```

LangChain의 `StructuredQuery` 표현을 그대로 Pinecone에 전달할 수는 없어서 translator가 필요했다. translator는 `Comparison`, `Operation` 같은 LangChain 공통 객체를 Pinecone의 `$eq`, `$in`, `$and`, `$or` filter 딕셔너리로 바꾼다.

```python
from langchain_core.structured_query import (
    Comparator,
    Comparison,
    Operation,
    Operator,
    StructuredQuery,
    Visitor,
)


class PineconeFilterTranslator(Visitor):
    allowed_comparators = (
        Comparator.EQ,
        Comparator.NE,
        Comparator.LT,
        Comparator.LTE,
        Comparator.GT,
        Comparator.GTE,
        Comparator.IN,
        Comparator.NIN,
    )
    allowed_operators = (Operator.AND, Operator.OR)

    def _format_function(self, function) -> str:
        self._validate_func(function)
        return f"${function.value}"

    def visit_comparison(self, comparison: Comparison) -> dict:
        value = comparison.value
        if comparison.comparator in (Comparator.IN, Comparator.NIN) and not isinstance(value, list):
            value = [value]
        return {
            comparison.attribute: {
                self._format_function(comparison.comparator): value
            }
        }

    def visit_operation(self, operation: Operation) -> dict:
        return {
            self._format_function(operation.operator): [
                argument.accept(self) for argument in operation.arguments
            ]
        }

    def visit_structured_query(
        self, structured_query: StructuredQuery
    ) -> tuple[str, dict]:
        if structured_query.filter is None:
            return structured_query.query, {}
        return structured_query.query, {
            "filter": structured_query.filter.accept(self)
        }
```

이 과정을 보면서 metadata schema 설명을 정확하게 주는 이유도 알게 됐다. `author`는 한 명의 문자열이고, `category`는 여러 값을 담는 `list[string]`이라는 정보를 LLM에 알려야 `김민수`, `AI 또는 건강` 같은 질문을 무리 없이 구조화할 수 있다. 또 `enable_limit=True`를 설정하면 `2개만`처럼 자연어에 포함된 수량 조건도 기본 `k=5`보다 우선할 수 있다.

```python
from langchain_classic.chains.query_constructor.schema import AttributeInfo
from langchain_classic.retrievers.self_query.base import SelfQueryRetriever

metadata_field_info = [
    AttributeInfo(
        name="author",
        description="문서 작성자 한 명",
        type="string",
    ),
    AttributeInfo(
        name="category",
        description="문서의 카테고리 목록",
        type="list[string]",
    ),
]

self_query_retriever = SelfQueryRetriever.from_llm(
    llm=chat_model,
    vectorstore=vector_store,
    document_contents="지식 문서의 본문",
    metadata_field_info=metadata_field_info,
    structured_query_translator=PineconeFilterTranslator(),
    search_kwargs={"k": 5},
    enable_limit=True,
)
```

Self-query는 편리하지만 LLM 호출이 하나 더 늘어난다. filter가 틀리게 생성되면 정답 문서를 아예 검색 후보에서 제외할 수도 있다. 그래서 `query_constructor`가 만든 `StructuredQuery`, translator가 바꾼 Pinecone filter, 최종 `list[Document]`를 따로 출력해 검증해야 한다. 그리고 Dense와 Self-query를 같은 qrels로 비교해 filter 적용이 실제 품질을 높였는지도 평가해야 한다. 오늘은 그 평가 함수와 비교 흐름까지 구성했고, 다음에는 질의별 오류 사례를 중심으로 결과를 확인해 보고 싶다.

## 실행 환경에서 만난 JPype 오류도 기록해 둔다

한국어 BM25를 위해 `konlpy`를 설치하면서 JPype와 Java를 함께 사용했다. 실행 중 Java가 `java.lang.System::load`의 native access 경고를 냈고, 종료 시점에는 `hs_err_pid33871.log`가 생성됐다. 로그상 Java 자체의 일반 예외라기보다, 종료 신호를 처리하던 JPype가 Python 3.12 네이티브 코드에서 `SIGSEGV`로 충돌한 경우였다.

```text
SIGTERM handler
→ org.jpype.JPypeSignal.interruptPy
→ python3.12 PyObject_RichCompareBool
→ SIGSEGV
```

학습 결과나 Pinecone에 넣은 문서가 망가졌다는 뜻은 아니고, Java와 Python을 연결한 커널 종료 과정의 충돌 로그다. 재발하면 JPype·KoNLPy 버전과 Java 버전을 확인하고, Jupyter kernel을 재시작한 뒤 같은 실습을 다시 실행해 보는 것이 좋겠다. 오늘처럼 native library를 쓰는 도구는 코드만 맞춰도 실행 환경 조합에 따라 문제가 생길 수 있다는 점도 기억해야겠다.

오늘 내용을 한 문장으로 정리하면, 좋은 RAG 검색은 vector similarity 하나를 선택하는 일이 아니라, 데이터와 질문의 특성에 맞는 검색기·결합 방식·metadata 조건을 정하고 qrels 지표로 검증하는 과정이라는 점이다.

다음에는 다음을 해 보고 싶다.

- RRF의 `rank_constant`와 BM25·Dense 후보 수를 바꿔 지표가 실제로 개선되는지 비교하기
- 고유명사·동의어·다중 정답 질의를 나누어 BM25와 Dense가 실패하는 사례를 직접 모아보기
- Self-query가 만든 `StructuredQuery`와 Pinecone filter를 질의별로 저장해 오변환 사례 확인하기
- 저자와 카테고리 조건이 함께 있는 질의에서 `$and`, `$or`, `$in` 결과가 기대와 같은지 테스트하기
- metadata filter 때문에 정답이 빠지는 경우를 포함해 Dense와 Self-query의 P@5, R@5, MRR, MAP을 다시 비교하기
