# 2026년 8월 11일 회고

오늘은 LangChain에서 Agent와 Tool을 연결하는 흐름을 살펴본 뒤, 그 흐름이 RAG로 어떻게 이어지는지 확인한 날이었다. Agent 관련 노트북에 날씨 조회, Tavily 검색, 여러 Tool을 함께 쓰는 Agent와 LCEL 파이프라인을 추가했고, 별도로 `06_rag/01_rag.ipynb`에는 PDF를 읽어 검색 가능한 RAG로 만드는 과정을 정리했다.

처음에는 코드를 따라 쓰면 실행은 되는데, `docs` 같은 이름이 어디서 생기고 왜 다시 같은 이름에 들어가는지부터 막혔다. 오늘 질문을 여러 번 하면서 느낀 것은 LangChain 자체가 어려운 것도 있지만, 그 전에 Python의 `변수`, `함수 반환값`, `객체`, `입력과 출력`을 한 줄씩 읽는 습관이 더 먼저 필요하다는 점이었다.

## Agent는 모델이 도구를 쓰게 만드는 실행 흐름

LLM은 문장을 만들 수는 있어도, 현재 서울 날씨나 최신 웹 문서를 스스로 확인할 수는 없다. 이때 외부 기능을 함수로 만들고 Tool로 공개하면, 모델이 질문을 읽은 뒤 어떤 함수를 호출할지 결정할 수 있다.

여기서 역할을 구분하는 것이 중요했다.

- `Tool`: 날씨 조회, 검색, 계산처럼 한 가지 작업을 하는 함수이다.
- `Tool Calling`: 모델이 함수 이름과 인자를 구조화된 형태로 요청하는 기능이다.
- `Agent`: 모델의 요청을 받아 실제 Python 함수를 실행하고, 결과를 다시 모델에 전달하는 실행 흐름이다.
- `Pydantic schema`: Tool이 어떤 인자를 받을지 검사하는 입력 규칙이다.

날씨 Tool에서는 `city`와 `units`의 규칙을 먼저 만들었다. 처음에는 `Field()`의 `discriminator` 자리에 단위 설명을 넣어서 오류가 났다. `discriminator`는 여러 모델이 섞인 Union을 구분할 때 쓰는 설정이고, 단순 설명은 `description`에 넣어야 했다.

```python
from typing import Literal

from pydantic import BaseModel, Field


class WeatherInput(BaseModel):
    city: str = Field(description="날씨를 조회할 도시 영문 이름. 예: Seoul, Busan")
    units: Literal["metric", "imperial"] = Field(
        default="metric",
        description="metric은 섭씨, imperial은 화씨 단위",
    )
```

`Literal`은 `units`에 아무 문자열이나 들어오는 것을 막아 준다. 모델이 `kelvin`처럼 정해지지 않은 값을 만들거나, 사람이 직접 Tool을 호출할 때 잘못된 값을 넣으면 API를 요청하기 전에 막을 수 있다.

날씨 API의 응답은 JSON이지만 `response.json()`을 호출하면 Python의 `dict`가 된다. 다만 OpenWeatherMap의 응답 전체를 Agent에 넘기면 필요 없는 정보가 많아서, 실제 답변에 필요한 값만 골라 반환하는 것이 더 좋았다.

```python
@tool(args_schema=WeatherInput)
def get_current_weather(city: str, units: str = "metric") -> dict:
    """OpenWeatherMap에서 지정한 도시의 현재 날씨를 조회한다."""
    response = requests.get(
        "https://api.openweathermap.org/data/2.5/weather",
        params={"q": city, "units": units, "appid": OPENWEATHER_API_KEY},
        timeout=10,
    )
    response.raise_for_status()

    data = response.json()
    return {
        "city": data["name"],
        "weather": data["weather"][0]["description"],
        "temperature": data["main"]["temp"],
        "feels_like": data["main"]["feels_like"],
        "humidity": data["main"]["humidity"],
        "unit": "°C" if units == "metric" else "°F",
    }
```

`return response.json()`도 문법적으로 맞지만, Agent가 읽을 Tool 결과까지 고려하면 필요한 데이터만 반환하는 편이 답변 품질과 확인하기 쉬운 정도 모두에 도움이 된다. `raise_for_status()`와 `timeout`도 함께 넣어 API 오류와 무한 대기를 대비했다.

## Tavily와 여러 Tool을 하나의 Agent에 연결하기

Tavily는 최신 웹 정보를 검색하는 Tool이다. `max_results`, `topic`, `search_depth`, `include_answer` 설정은 모두 실제 검색 결과의 범위와 양을 정한다. API 키는 `.env`에서 읽고 코드에 직접 쓰거나 출력하지 않는 원칙도 다시 확인했다.

여러 Tool을 연결하면 Agent가 질문에 맞는 기능을 골라 호출한다. `tools` 목록에 넣었다고 모든 Tool이 매번 실행되는 것은 아니다.

```python
from langchain.agents import create_agent

multi_tool_agent = create_agent(
    model=model,
    tools=[
        search_wikipedia,
        search_arxiv,
        get_current_weather,
        tavily_search,
        calculate_age,
    ],
    system_prompt=(
        "질문에 맞는 실제 조회 도구를 사용한다. "
        "도구가 반환한 값만 근거로 답하며 URL이 있으면 출처로 표시한다."
    ),
)
```

Agent 실행 결과를 그대로 `print()`하면 사용자 메시지, Tool 호출 요청, Tool 결과, 최종 AIMessage가 모두 출력되어 길어 보인다. 사용자에게 보여 줄 답만 필요하면 마지막 메시지의 `content`를 확인하면 된다.

```python
result = multi_tool_agent.invoke(
    {"messages": [{"role": "user", "content": "서울의 현재 날씨를 알려 줘."}]}
)

print(result["messages"][-1].content)
```

다만 중간 과정을 모두 버리기 전에, 학습과 디버깅 중에는 `tool_calls`와 `ToolMessage`를 확인해야 한다. 최종 문장이 그럴듯해도 Agent가 정말 Tool을 호출했는지, 어떤 인자를 사용했는지는 중간 메시지에서만 알 수 있기 때문이다.

## LCEL의 `|`는 앞 결과를 다음 입력으로 전달한다

오늘 가장 헷갈렸던 부분은 `Prompt | Agent | Parser`처럼 보이는 LCEL 체인이었다. `|`는 그냥 기호를 이어 쓰는 문법이 아니라, 왼쪽 결과를 오른쪽 입력으로 전달하는 의미였다. 그래서 각 단계의 자료형을 읽어야 한다.

```text
{"question": "서울 날씨"}
→ ChatPromptValue
→ {"messages": [HumanMessage(...)]}
→ Agent 실행 결과 dict
→ AIMessage
→ str
```

`ChatPromptTemplate.format_messages()`는 템플릿을 만드는 함수가 아니라 이미 만들어진 템플릿에 값을 채우는 함수라는 것도 구분했다. 먼저 `from_messages()`로 틀을 만들고, 실행할 때 `invoke()`에 `question` 값을 전달해야 한다.

```python
agent_prompt = ChatPromptTemplate.from_messages([
    ("human", "{question}\n최종 답변은 두 문장으로 작성한다."),
])
```

`create_agent()`가 만든 `CompiledStateGraph`는 실행할 때 `.invoke()`를 사용할 수 있지만, Pylance는 이를 일반 Runnable처럼 인식하지 못해 `|` 연산자 타입 오류를 보여 주기도 했다. 이 경우 Agent를 실행하는 함수를 `RunnableLambda`로 감싸면 파이프라인의 입력과 출력이 더 명확해진다.

```python
from typing import Any, cast

from langchain_core.runnables import RunnableLambda


def prompt_to_agent_input(prompt_value) -> dict[str, Any]:
    return {"messages": prompt_value.to_messages()}


def invoke_multi_tool_agent(agent_input: dict[str, Any]) -> dict[str, Any]:
    result = multi_tool_agent.invoke(cast(Any, agent_input))
    return cast(dict[str, Any], result)


agent_pipeline = (
    agent_prompt
    | RunnableLambda(prompt_to_agent_input)
    | RunnableLambda(invoke_multi_tool_agent)
    | RunnableLambda(lambda result: result["messages"][-1])
    | StrOutputParser()
)
```

이 코드를 이해할 때는 한 줄 전체를 외우기보다, 각 단계가 무엇을 받고 무엇을 내보내는지 적어 보는 편이 낫다. `RunnableLambda`는 일반 Python 함수를 LangChain의 Runnable처럼 연결할 수 있게 감싸는 도구다.

## RAG는 검색한 문서를 근거로 답하게 만드는 방식

오늘 새로 만든 `06_rag/01_rag.ipynb`에서는 백설공주 PDF를 대상으로 기본적인 `2-step RAG`를 구성했다. RAG는 `Retrieval-Augmented Generation`의 약자로, 모델이 기억만으로 답하는 대신 먼저 관련 문서를 검색하고 그 문서를 근거로 답하게 만드는 방식이다.

```text
사용자 질문
→ Retriever가 관련 Document 검색
→ 검색 문서를 context로 정리
→ Prompt에 질문과 context 전달
→ LLM이 근거 기반 답변 생성
```

Retriever는 답변을 만드는 객체가 아니다. 질문을 받고 관련 있는 `list[Document]`를 반환하는 검색 인터페이스다. LLM은 Retriever가 가져온 문서를 읽고 답변을 만든다. 이 역할을 분리해서 생각하니 RAG가 조금 더 명확해졌다.

PDF를 읽으면 페이지별 Document 목록이 만들어진다. 이후 Text Splitter가 긴 문서를 작은 chunk로 나누고, Embedding Model이 chunk를 숫자 배열로 바꾸고, Chroma Vector Store가 vector와 원문을 함께 저장한다.

```text
PDF
→ list[Document]
→ list[chunk Document]
→ embedding vectors
→ Vector Store
→ Retriever
```

## 같은 `docs`를 다시 쓰는 이유를 이해하기

Python 문법에서 가장 오래 멈춘 부분은 아래 코드였다.

```python
docs = splitter.split_documents(docs)
```

왼쪽 `docs`와 오른쪽 `docs`가 같은 이름이라 이상하게 보이지만, Python은 오른쪽을 먼저 계산한 뒤 그 결과를 왼쪽 이름에 저장한다. 이미 `docs`가 PDF 페이지 목록을 가리키고 있다면, `split_documents(docs)`는 그 목록을 받아 잘게 나눈 새로운 목록을 돌려준다. 마지막에 `docs`라는 이름이 새 목록을 가리키게 되는 것이다.

```text
실행 전 docs: [페이지 1 Document, 페이지 2 Document, ...]
실행 후 docs: [chunk 1, chunk 2, chunk 3, ...]
```

파이썬에서 자주 쓰는 방식이지만, 학습 단계에서는 같은 이름을 계속 쓰면 역할이 흐려진다. 그래서 아래처럼 `page_docs`와 `chunk_docs`를 나누어 쓰는 편이 더 이해하기 좋다고 느꼈다.

```python
page_docs: list[Document] = []

for page_index, page in enumerate(reader.pages):
    page_docs.append(
        Document(
            page_content=page.extract_text() or "",
            metadata={"page": page_index + 1},
        )
    )

chunk_docs = splitter.split_documents(page_docs)
```

여기서 `page_docs.append(...)`는 기존 리스트 안에 항목을 추가하는 동작이고, `chunk_docs = ...`는 함수가 반환한 새 목록을 변수에 저장하는 동작이다. 둘은 비슷하게 보여도 다르다. Notebook에서 마지막 줄에 `docs`만 적었을 때 값이 보이는 것은 Notebook이 마지막 표현식의 결과를 자동 출력하기 때문이며, 일반 `.py` 파일에서는 `print(docs)`를 써야 한다.

## 오늘의 정리와 다음에 확인할 점

오늘은 Agent와 RAG를 따로 배운 것 같지만, 결국 둘 다 데이터가 어디서 와서 어디로 가는지 읽는 문제였다. Agent에서는 `사용자 메시지 → Tool 호출 → Tool 결과 → 최종 답변`을 따라가고, RAG에서는 `질문 → Retriever → Document → context → 답변`을 따라간다.

아직은 `ChatPromptValue`, `AIMessage`, `dict`, `list[Document]`처럼 자료형이 바뀌는 지점에서 바로 이해되지 않는다. 하지만 모르는 상태에서 코드를 그대로 이어 붙이기보다는, 변수 이름을 역할에 맞게 바꾸고 각 줄을 오른쪽부터 읽는 방식으로 확인해야겠다.

다음에는 다음을 직접 해 보고 싶다.

- `page_docs`와 `chunk_docs`를 구분한 RAG 코드로 다시 실행하기
- Retriever가 반환한 Document의 `page_content`와 `metadata`를 직접 출력해 보기
- 검색 결과가 질문의 답을 포함하지 않을 때 LLM이 모른다고 답하는지 확인하기
- Agent의 최종 답만 보지 않고 `AIMessage.tool_calls`와 `ToolMessage`를 같이 확인하기
- Mermaid 다이어그램이 VS Code Notebook에서 렌더링되는 환경과 버전을 점검하기

오늘 배운 내용을 한 문장으로 정리하면, LangChain의 문법을 외우기 전에 각 단계의 입력과 출력이 무엇인지 읽는 것이 먼저라는 점이다. 이 기준으로 보면 Agent와 RAG도 복잡한 기능 묶음이 아니라, 필요한 정보를 가져와 다음 단계에 전달하는 흐름으로 조금씩 보이기 시작한다.
